# 处理器微架构深度指南系列 —— 内容完善计划

## 背景

看过 leetcpu 中文站现有的 7 个模块页面（缓存层次、ILP 与流水线、内存层次与延迟隐藏、ROB 与乱序执行、前端与指令缓存、TLB 与地址转换、SIMD 与向量化），它们目前都是同一种"轻量级"结构：一段导语 + 3 个概念小节（每节 2-4 句话）+ 一张关键指标表 + 配套题目列表。信息密度远低于我们已经完成的《分支预测完全指南》——那篇有实验数据推导、硬件全景、算法演进史、真实芯片参数对比四个层次，累计约 450 行。

这份计划的目标：把 7 个主题都补充到同一深度，同时**贴合现代处理器的真实情况**（不是教科书里 2000 年代的抽象模型，而是 Zen 5 / Alder Lake-Redwood Cove / Apple M4 / Neoverse V2 这一代的实际参数）。

## 统一的写作框架（复用分支预测指南的四段式）

每篇文章建议固定四个部分，保证系列风格一致：

| 部分 | 内容 | 目的 |
|---|---|---|
| **Part 1：现象与量化案例** | 挑一个 leetcpu 已有的配套题目（或设计一个新的最小可复现实验），给出具体数字：耗时、命中率、MPKI/IPC 等指标的推导过程 | 让读者先"感受到"问题的量级，而不是上来就讲理论 |
| **Part 2：硬件全景** | 该主题涉及的硬件结构、它们在流水线中的位置、彼此如何协作/竞争资源 | 建立结构性认知，画出取指→译码→执行→retire 全链路里这一部分的位置 |
| **Part 3：概念/算法演进史** | 从最早的朴素方案到现代方案的演进脉络，每一步解决什么问题、引入什么代价 | 避免"记结论不知道为什么"，也顺带覆盖历史上淘汰的思路（避免读者重新发明它们） |
| **Part 4：真实处理器对比** | 至少覆盖 Intel（Redwood Cove/Lion Cove）、AMD（Zen 4/5）、ARM（Neoverse V2/N3, Cortex-X 系列）、Apple（M3/M4 系列）四家当前一代的具体参数表，注明数据来源 | 落地到"我用的这颗 CPU 到底是什么样"，这是与教科书内容拉开差距的部分 |

**质量约束（沿用分支预测指南review中发现的教训）**：
- 每个数字必须有可查来源（厂商优化手册、WikiChip、Chips and Cheese、ISCA/MICRO论文），标注出处，不确定的标"待核实"而不是编造
- 状态图/流程图画完后要逐状态自查一遍，避免像 2-bit 计数器那样漏画状态
- 结论性论断（如"没有任何算法能做到 X"）要显式给出适用条件，不要让读者过度泛化
- 凡是引用学术论文的具体数字（如 PHR 长度、表项数），至少交叉验证两个信息源

## 分主题计划

### 1. 缓存层次（优先级：高，建议第一个做）

**现状**：已有 7 道配套题目（分块、AoS→SoA、stencil、Eytzinger 布局等），素材最丰富，概念讲解只有"层次结构/局部性/如何减少失效"三小节。

**Part 1 案例候选**：用 02 题（矩阵乘法分块）做量化案例——朴素三重循环 vs 分块版本，具体算出工作集大小、L1/L2/LLC 命中率变化、理论 FLOPS 上限（roofline 视角）。

**Part 2 硬件全景要点**：
- 现代三级/四级结构：L1D/L1I 分离、私有 L2、共享 LLC（部分产品还有 L3.5/System Level Cache，如 Apple 的 SLC）
- 组相联结构（way、set、tag）、替换策略（LRU 近似、RRIP）
- 缓存一致性协议简介（MESI/MOESI）对多核场景的影响（伪共享 false sharing 值得单独展开，是极常见的真实性能坑）
- 非包含式 vs 包含式 vs 非独占式（non-inclusive）层次结构，Intel 从 Skylake-X 开始转向 non-inclusive LLC 的影响

**Part 3 演进史**：直接映射缓存 → 组相联 → 替换策略演进（LRU→Pseudo-LRU→RRIP/SRRIP，2010 年后学术界的 DRRIP 等）→ 预取器的加入如何改变"命中率"这个指标的意义。

**Part 4 真实数据要点（需搜索核实）**：
- Intel Redwood Cove/Lion Cove：L1D 48KB、L2 私有容量大幅增加（Meteor Lake/Lunar Lake 到 2.5MB/3MB 级别）
- AMD Zen 4/5：L1D 32KB、L2 1MB、3D V-Cache 型号的 L3 96MB+ 现象级容量，及其对特定负载（游戏、EDA）的实测收益
- Apple M-系列：L1D 128KB（已在分支预测文档里核实过）、系统级缓存（SLC）容量与共享特性
- ARM Neoverse V2：L1D 64KB、可配置的 L2/L3

**建议篇幅**：中等偏长（伪共享+一致性协议值得展开，篇幅可能接近分支预测指南）。

---

### 2. ILP 与流水线（优先级：高）

**现状**：6 道配套题目（规约树、popcount、直方图、多累加器等），概念讲解覆盖乱序执行、依赖链、提升 IPC 的手段，但完全没有涉及"流水线"本身的结构（取指-译码-执行-访存-写回分级、超标量、发射宽度）。**这是标题和内容不匹配的地方，值得在完善时补齐"流水线"这半个主题**。

**Part 1 案例候选**：用 16 题（多累加器）或 21 题（八累加器点积）做案例，结合具体浮点加法延迟（如 FMA 延迟 4 周期、吞吐 0.5 周期），推导单累加器 IPC 上限公式：IPC ≈ 1/延迟，并算出理论 4/8 累加器版本的加速比与实测差距（受限于发射宽度、寄存器重命名等）。

**Part 2 硬件全景要点**：
- 经典 5 级流水线 → 现代 15-20 级深流水线，为什么深流水线换来高频但加大误预测代价（与分支预测指南呼应）
- 超标量宽度：现代核的取指/译码/发射/退休宽度（如 Apple M 系列 8-9 宽译码，远超 x86 的 4-6 宽，这是很有意思的对比点）
- 寄存器重命名（Register Renaming）解决 WAR/WAW 假相关的机制，物理寄存器堆大小
- 执行端口与调度队列（Reservation Station / Scheduler），端口绑定造成的资源冲突

**Part 3 演进史**：单发射顺序执行 → 超标量顺序 → Tomasulo 算法与乱序执行的引入 → 现代分布式调度队列。可以提一下 IBM 360/91 是 Tomasulo 算法的起点。

**Part 4 真实数据要点**：
- 各厂商当前一代的译码宽度、执行端口数、物理寄存器堆大小（Intel Lion Cove 采用双译码集群设计，是最近一代很值得讲的架构变化）
- AMD Zen 5 的双译码集群（dual decode cluster）新设计
- Apple Firestorm/Everest 的宽译码优势来源分析

**建议篇幅**：长（流水线内容需要新写，不是简单扩展）。

---

### 3. 内存层次与延迟隐藏（优先级：中，紧接缓存层次之后）

**现状**：3 道配套题目（指针追逐、跨步求和预取、不规则聚集预取），概念讲解覆盖延迟 vs 带宽、软硬件预取、DRAM 行缓冲，MLP（内存级并行）是核心概念但只有一句话带过，值得展开。

**Part 1 案例候选**：04 题指针追逐——单链表 vs N 路交错链表，用 Little's Law（吞吐 = 并发请求数 / 延迟）具体算出交错度与吞吐的关系曲线。

**Part 2 硬件全景要点**：
- Line Fill Buffer / MSHR（Miss Status Holding Register）数量决定了"同时能有多少个未完成的缓存失效"，这是 MLP 的硬件上限，非常值得单独讲清楚（很多人不知道这个瓶颈的存在）
- DRAM 结构：bank/row/column，多通道、多 rank 如何提供并行度
- 内存控制器调度策略（FR-FCFS 等）
- 硬件预取器分类：stride、stream、pattern-based（如 Intel 的 DCU/IP-based prefetcher，AMD 的多级预取器）

**Part 3 演进史**：从"CPU 直接等 DRAM"到多级缓存出现，再到 MLP 概念的提出（Glew 1998 的"missing status holding register"研究、Mutlu 等人关于 MLP-aware 调度的工作）、再到现代预取器的机器学习化趋势（如部分厂商开始用简单的表格学习访问模式）。

**Part 4 真实数据要点**：
- 各平台 MSHR/LFB 数量（Intel 官方很少公开，需要靠逆向工程论文如 membench 类工作）
- 内存带宽/延迟实测数字（DDR5 vs LPDDR5X，Apple 统一内存架构的带宽优势）
- Apple M 系列的统一内存架构（UMA）与 CPU/GPU/NPU 共享带宽的取舍

**建议篇彩**：中等，MSHR 数量这类数据比较难找到官方确认来源，需要谨慎标注"来自逆向工程测量，非官方数字"。

---

### 4. ROB 与乱序执行（优先级：中）

**现状**：只有 1 道配套题目（瓶颈会诊），概念讲解质量不错但明显单薄，是 7 个模块里最需要补充的。

**Part 1 案例候选**：05 题瓶颈会诊本身就是"访存跨行+分支随机+冗余依赖"三重瓶颈叠加的场景，适合直接用来演示"如何用 ROB 占用率 + IPC + 误预测时 ROB 占用三个指标交叉定位问题"。

**Part 2 硬件全景要点**：
- ROB 具体结构：条目内容（指令、目的寄存器、异常状态、完成标志）、头尾指针如何推进
- 与调度队列（Scheduler/RS）、物理寄存器堆、Load/Store Queue 的关系——ROB 只负责顺序退休，真正的乱序发射靠调度队列
- Load/Store Queue 的内存消歧（memory disambiguation）机制，与 ROB 协同处理内存序
- 精确异常（precise exception）为什么必须靠顺序退休来保证

**Part 3 演进史**：从 Tomasulo 的 CDB（Common Data Bus）广播机制，到现代 ROB+物理寄存器堆（PRF）设计（P6 引入 ROB，后续演进为 PRF-based 设计，摆脱了早期 ROB 兼做数据存储的开销）。

**Part 4 真实数据要点**：
- ROB 大小的世代演进：这是最好拿到公开数字的一类参数（Intel 从 Skylake 224 到 Golden Cove 512+、Lion Cove 更大；AMD Zen 4 320 到 Zen 5 448；Apple M 系列早就以 630+ 著称，是很好的对比素材）
- 各家调度队列结构差异（统一调度队列 vs 分布式端口专属队列）

**建议篇幅**：长，这是与 ROB 大小这类"有梗"的对比数据最丰富的话题之一，值得重点投入。

---

### 5. 前端与指令缓存（优先级：中，与分支预测指南有大量重叠需要交叉引用）

**现状**：概念讲解完整但**没有配套题目**（页面写"该模块暂无直接配套题目"），Part 1 需要额外设计案例或从其他模块借用（比如展开过度导致 I-cache 压力增大的反例）。

**Part 1 案例候选**：需要新设计一个实验——比如同一个循环体分别以不同展开倍数编译，测量 L1I MPKI 与 uop cache 命中率的变化，展示"过度展开反而伤前端"的真实反例（呼应 ILP 模块里"展开有益"的结论，形成有趣的张力）。

**Part 2 硬件全景要点**：
- 取指流水线细节：BTB → 预译码 → I-cache 取指 → 译码 → uop cache 写入，与分支预测指南 Part 2 的 BTB 内容直接衔接，可以互相链接而不是重复讲
- uop cache / DSB（Decoded Stream Buffer）：命中时完全跳过译码，是现代 x86 前端最大的优化之一；ARM/Apple 因为指令集更规整，对 uop cache 的依赖程度不同，值得对比
- 微指令融合（macro-op fusion）与宏操作融合

**Part 3 演进史**：从简单顺序取指，到分支预测辅助取指，到 uop cache 的引入（Sandy Bridge 首次引入 DSB）解决 x86 变长指令译码开销大的问题；对比 ARM/RISC-V 这类定长指令集从一开始译码开销就更小，是否还需要 uop cache（现代 ARM 大核其实也开始引入类似机制，是个值得深挖的点）。

**Part 4 真实数据要点**：
- Intel uop cache 容量演进（Sandy Bridge 1.5K uop 到 Lion Cove 显著增大）
- AMD Zen 系列 op cache 容量
- ARM/Apple 是否有类似结构、L1I 容量对比（Apple 192KB L1I 这个数字很惊人，值得核实并强调）

**建议篇幅**：中等，注意跟分支预测指南 Part 2/4 做好分工，避免整段重复。

---

### 6. TLB 与地址转换（优先级：较低，但话题独立性强，适合单独成篇）

**现状**：只有 1 道配套题目（聚集重排），概念讲解清晰但完全没有涉及"多级页表遍历"的具体机制、大页、以及虚拟化场景下的两层地址转换（这在云计算场景下非常现实）。

**Part 1 案例候选**：19 题聚集重排——用具体页大小（4KB）和数组规模算出"随机访问 vs 按页排序后访问"跨越的页数差异，进而推算 TLB 覆盖范围（如 L1 dTLB 64 项 × 4KB = 256KB vs 覆盖不到的工作集）。

**Part 2 硬件全景要点**：
- 多级页表结构（x86-64 四级/五级页表，PML4→PDPT→PD→PT）与硬件页表遍历器（Page Table Walker）
- 多级 TLB：L1 dTLB/iTLB（几十项，全相联，1周期）→ L2 STLB（上千项，几个周期）
- 大页（2MB/1GB Huge Page）如何减少页表层级、增大 TLB 覆盖
- 虚拟化场景下的两层地址转换（Guest Virtual→Guest Physical→Host Physical）与 EPT/NPT，以及第二层 TLB（如 Intel 的 EPT walk 开销），这是云原生时代很值得单独提一笔的现实话题

**Part 3 演进史**：从单级 TLB 到多级 TLB，从固定 4KB 页到透明大页（THP）自动管理的引入（Linux 2.6.38），再到虚拟化普及后 nested paging 硬件支持的加入（Intel EPT 2008、AMD NPT 更早）。

**Part 4 真实数据要点**：
- 各家 L1/L2 TLB 项数对比（Intel、AMD、Apple、ARM 具体数字，需要查优化手册）
- 大页对 TLB 覆盖范围的量化提升（4KB→2MB 是 512 倍覆盖率提升）

**建议篇幅**：中等，虚拟化两层地址转换这块内容新颖但要控制篇幅避免喧宾夺主。

---

### 7. SIMD 与向量化（优先级：中高，内容变化最快，最需要"贴合现代处理器"）

**现状**：概念讲解合格但**没有配套题目**，且完全没有点名任何具体指令集（SSE/AVX/AVX-512/AVX10/NEON/SVE/SVE2），这是 7 个模块里"现代性"缺失最严重的一个——SIMD 领域这两年变化极快（AVX10、SME 矩阵扩展等）。

**Part 1 案例候选**：需要新设计——例如用 18 题 SAXPY 的自动向量化版本，对比标量/SSE(128位)/AVX2(256位)/AVX-512(512位)/理论 AMX 矩阵指令的吞吐差异，具体到"同样数据量下，向量宽度每翻倍、理论加速比如何变化，实际测得的加速比为什么达不到理论值（内存带宽瓶颈接管）"。

**Part 2 硬件全景要点**：
- 向量寄存器堆、执行端口宽度与向量指令的关系（AVX-512 在部分 Intel 型号上会降频，"AVX-512 频率惩罚"是个很真实的现代话题）
- 掩码寄存器（AVX-512 的 k0-k7、ARM SVE 的 predicate 寄存器）如何实现"可变长度向量+条件执行"，这是取代传统"标量分支+SIMD"混合写法的关键机制
- Gather/Scatter 指令：对不连续内存的向量化支持，代价远高于连续访问，需要具体量化

**Part 3 演进史**：MMX(1997,64位) → SSE系列(128位) → AVX/AVX2(256位) → AVX-512(512位，2016 Skylake-X 引入，因功耗/频率问题几经反复) → **AVX10（2023年 Intel 提出的统一方案，试图解决 AVX-512 生态碎片化问题，这是最新进展，务必包含）**；ARM 侧 NEON(128位,固定) → SVE/SVE2(可变长度，128-2048位，"一次编译到处跑"的设计哲学，与 x86 路线形成有趣对比)。矩阵扩展新趋势：Intel AMX、ARM SME，为 AI workload 专门设计的指令集，是 2023 年后最重要的新增内容。

**Part 4 真实数据要点**：
- 各家当前支持的最高 SIMD 宽度与具体型号对应关系（哪些 Intel 型号支持 AVX-512、哪些因为 E-core 混合架构反而阉割了 AVX-512——这是桌面端 Alder Lake 后一段時間的真实历史事件，值得讲）
- Apple M 系列 NEON 实现宽度与 AMX（Apple 自己的矩阵协处理器，与 Intel AMX 同名不同物，容易搞混，需要在文档中明确区分）
- ARM Neoverse V2 的 SVE2 向量长度（通常 128 位实现，尽管 ISA 支持到 2048 位，这是"ISA 灵活性 vs 实际实现"值得强调的落差）

**建议篇幅**：长，且是 7 个模块里最需要额外查证的（指令集历史变化快，容易有过时信息）。

---

## 建议的执行顺序与依赖关系

```
分支预测（已完成）
      │
      ├──► 前端与指令缓存   （复用分支预测 Part2/4 的 BTB 内容，工作量较小）
      │
缓存层次 ──► 内存层次与延迟隐藏 ──► TLB 与地址转换
      │            （三者是同一条访存链路，建议连续完成，术语和数据可以互相引用）
      │
ILP 与流水线 ──► ROB 与乱序执行 ──► SIMD 与向量化
      （三者是同一条执行侧链路：流水线基础 → 乱序调度机制 → 向量化执行单元）
```

建议顺序：**缓存层次 → ILP 与流水线 → 内存层次与延迟隐藏 → ROB 与乱序执行 → 前端与指令缓存 → SIMD 与向量化 → TLB 与地址转换**。前五个内容关联性强、素材（配套题目）充足，能较快上手；SIMD 需要较多额外查证；TLB 相对独立，放最后不影响其他篇目。

## 每篇的核查清单（写完后自查用）

- [ ] Part 1 的数字是否都能从代码逻辑或已知硬件参数推导出来，而不是凭感觉估的
- [ ] Part 2 的图/状态机是否画全了所有状态和转移路径
- [ ] Part 3 的历史脉络是否交代了"为什么旧方案不够用"，而不只是罗列时间线
- [ ] Part 4 的每个具体数字（容量、周期数、宽度）是否至少能找到一个可信来源，标注"来自 XXX，年份"
- [ ] 是否有当前一代（2024-2026 发布）的产品数据，而不是停留在 3-4 代前的经典架构
- [ ] 结论性的"总是/从不"类表述是否有例外条件需要说明
- [ ] 与其他篇目重叠的内容是否做了交叉引用而不是重复展开

如果这个计划方向没问题，我可以从"缓存层次"开始，按照分支预测指南的结构先写出完整初稿。
