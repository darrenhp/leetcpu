# -*- coding: utf-8 -*-
"""第 1–11 题的中文题面与优化报告。"""

PROBLEMS_ZH = {
    "stable-partition-branches": {
        "zh": "稳定划分：让分支变得可预测",
        "one_liner": "一次预处理，把随机分支改造成\"两段式\"可预测分支。",
        "bottleneck": "分支预测失败",
        "desc": [
            "对同一个包含 100,000 个整数的数组，重复回答同一个阈值查询 100 次。",
            "基线实现中，每一轮都在原始随机顺序的数组上扫描。由于分支 `arr[i] > 128` 在随机数据上接近 50/50，双模（bimodal）分支预测器会频繁猜错。",
            "你可以在 100 轮开始之前做**一次**预处理。重新排列数据，让这条热分支变得容易预测，同时保持所有数值与最终求和结果与参考实现完全一致。",
        ],
        "prompt": "实现 `long solve(int *arr, int n)`，返回 100 轮完整扫描中所有大于 128 的值之和。允许在重复扫描前做一次前置预处理。",
        "constraints": [
            "允许在重复查询之前做一次预处理",
            "必须精确保留所有原始数值",
            "输出必须等于参考实现在 100 轮上的总和",
            "不允许使用 SIMD intrinsics",
            "不允许多线程",
        ],
        "examples": [{
            "input": "arr = [10 万个 0–255 的随机整数]，REPEATS = 100",
            "output": "100 轮中所有 >128 的值之和",
            "explanation": "把\"所有 ≤128 的元素在前、所有 >128 的元素在后\"这样的稳定划分，就能把一条近乎随机的分支改造成两段很长的可预测分支。",
        }],
        "report": {
            "summary": "不做任何分支消除，而是**改变数据的排列顺序**：先把 ≤128 的元素全部搬到前面，再把 >128 的元素搬到后面。于是同一条分支在扫描过程中只会\"翻转一次\"，预测器几乎全程命中。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "热分支 `arr[i] > 128` 在随机数据上的真/假比例接近 1:1，双模预测器的历史位被反复冲刷，准确率退化到接近抛硬币。",
                    "分支误预测代价约 15 个周期；100 轮 × 10 万次判断中若有 5% 误预测，就是约 750 万个周期的纯浪费。",
                    "关键观察：判断条件只依赖元素值本身，与元素的位置无关 → 我们可以自由重排，而结果（求和）不受影响。",
                ]},
                {"h": "优化思路", "p": [
                    "用一次 O(n) 的稳定划分（stable partition）把数组分成两段：前段全部 ≤128，后段全部 >128。",
                    "划分后，扫描过程中分支先一直是\"假\"，到中点后一直是\"真\"，只翻转一次；预测器在每段内都能稳定命中。",
                    "预处理成本 O(n)，被 100 轮扫描摊薄，几乎可以忽略。",
                ]},
                {"h": "关键改动", "p": [
                    "分配临时数组 `tmp`，第一遍收集所有 `arr[i] <= 128`，第二遍收集所有 `arr[i] > 128`。",
                    "两趟收集保持了段内相对顺序（稳定），因此数值集合不变，整数求和满足交换律，结果与参考实现严格一致。",
                    "主体循环完全不动 —— 优化全部发生在循环之外，这正是\"数据布局优化\"的典型形态。",
                ]},
                {"h": "为什么更快", "p": [
                    "分支预测准确率从约 50% 提升到接近 100%，Branch MPKI 从数十降到个位数甚至更低。",
                    "流水线不再被周期性清空，后端能持续供给指令，IPC 随之上升。",
                    "额外代价只有一次 10 万元素的临时拷贝，相对于 100 轮 × 10 万次判断可以忽略。",
                ]},
                {"h": "常见误区", "p": [
                    "误以为必须先排序：排序是 O(n log n) 且做了多余的功；稳定划分 O(n) 就够，因为我们只需要\"两段\"而不是全序。",
                    "担心重排改变结果：求和是可交换的，只要元素集合不变，总和必然不变；但若题目依赖遍历顺序（如前缀操作）则不可这样做。",
                    "忽略预处理成本：若查询只做 1 轮，预处理反而是净亏损。这类优化的前提是\"一次整理、多次查询\"。",
                ]},
            ],
            "keypoints": ["数据重排优于分支消除（当两者都可行时）", "O(n) 预处理被 100 轮摊薄", "稳定划分保持元素集合不变"],
            "pitfalls": ["对只扫描一次的场景不要照搬", "浮点求和不可任意重排（本题是整数，安全）"],
        },
    },
    "matrix-multiply-tiling": {
        "zh": "矩阵乘法：分块与转置",
        "one_liner": "转置 B 消除列访问，再用分块把工作集压进缓存。",
        "bottleneck": "缓存空间局部性",
        "desc": [
            "计算两个 512×512 行优先 float 矩阵的乘积 `C = A × B`。",
            "基线实现按行读 A，却按**列**遍历 B，空间局部性极差并产生大量本可避免的缓存失效。",
            "请用分块、转置或两者结合重写循环嵌套，让内层循环以缓存友好的顺序读取内存。",
        ],
        "prompt": "实现 `void solve(float *A, float *B, float *C)`，精确计算两个 512×512 行优先矩阵的乘积，同时改善缓存局部性。",
        "constraints": ["N = 512（固定）", "矩阵为行优先 float", "不允许外部库（禁用 BLAS）", "不允许多线程", "允许 SIMD"],
        "examples": [{
            "input": "A、B 为 512×512 行优先 float 矩阵",
            "output": "C = A × B，与朴素实现逐位一致（含浮点容差）",
            "explanation": "内层 k 循环中，`B[k*N + j]` 的相邻迭代地址相差 N×4 = 2048 字节，即每次都换一条缓存行 —— 这正是要消除的访存模式。",
        }],
        "report": {
            "summary": "两步走：先**转置 B** 让内层循环变连续，再对 i/j/k 三重循环做 **64×64×64 分块**，让 A、BT、C 的数据块在计算期内留在缓存中被反复复用。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "内层 `C[i*N+j] += A[i*N+k] * B[k*N+j]` 中，k 变化时 `B[k*N+j]` 的地址每次跳 2048 字节 —— 每次访问一条新缓存行，而这条行里只用了 4 个字节。",
                    "总访存量看似只有 3 个矩阵 × 1MB，但按行计的访问模式会把有效带宽浪费 16 倍（64 字节行只用 4 字节）。",
                    "L1 MPKI 高达数十，IPC 被访存延迟压住。",
                ]},
                {"h": "优化思路", "p": [
                    "转置：预先构造 `BT[j*N+i] = B[i*N+j]`，使内层 `BT[j*N+k]` 沿 k 连续访问，一行 64 字节全部被利用。",
                    "分块：把 512×512×512 的迭代空间切成 64×64×64 的块。处理一个块时，A 的一块（64×64×4B=16KB）、BT 的一块（16KB）与 C 的一块（16KB）合计约 48KB，恰好贴近 L1 容量。",
                    "块内 k 循环把 `C[i][j]` 累加到寄存器变量 `sum` 中，避免每步写回内存。",
                ]},
                {"h": "关键改动", "p": [
                    "先用 O(N²) 的转置把 B 变成 BT —— 这是一次性成本，被 N³ 的计算完全摊薄。",
                    "三层 `ii/jj/kk` 分块循环（TILE=64）+ 三层块内循环，共六层嵌套。",
                    "把 `C[i*N+j]` 提升到寄存器 `sum`，块内 k 循环结束后一次性写回。",
                ]},
                {"h": "为什么更快", "p": [
                    "转置把 B 的缓存行利用率从 4/64 提升到 64/64，等价地把 B 侧的有效带宽提升约 16 倍。",
                    "分块让同一块 A 数据被 64 个 j 复用、同一块 BT 被 64 个 i 复用，把 O(N³) 的访存降到接近 O(N³/TILE)。",
                    "寄存器累加避免了 C 的重复读写，同时给编译器留出向量化空间。",
                ]},
                {"h": "常见误区", "p": [
                    "TILE 越大越好？不是。块太大装不进 L1 就会退化，通常要让 3 个块的总量略小于 L1 容量的一半到三分之二。",
                    "只转置不分块：能消除列访问，但 A、B 仍会被反复从 L2/LLC 重新读入，收益有限。",
                    "忽略边界：512 能被 64 整除，本题无需处理余数；实际工程中必须写清尾块处理。",
                ]},
            ],
            "keypoints": ["转置消除跨步访问", "六层分块循环", "寄存器累加 + 末尾一次写回"],
            "pitfalls": ["分块尺寸需按实际缓存容量调", "A、BT、C 三块合计不能超过 L1"],
        },
    },
    "reduction-tree-ilp": {
        "zh": "规约树：打断依赖链",
        "one_liner": "单累加器是一条长度为 N 的依赖链 —— 用 4 个累加器把它拆成 4 条。",
        "bottleneck": "浮点依赖链",
        "desc": [
            "对 100,000 个浮点数求和。基线实现只用一个累加器，于是每次加法都依赖上一次加法的结果。",
            "这条长依赖链使乱序核无法把多次加法重叠起来。",
            "请重构规约过程以制造彼此独立的工作（例如多累加器 + 循环展开），同时保持在允许的浮点误差范围内。",
        ],
        "prompt": "实现 `void solve(float *arr, float *out)`，使 `out[0]` 成为 10 万个 float 之和，且循环结构比单累加器暴露出更多指令级并行。",
        "constraints": ["结果与参考值误差需在 fp 容差 1e-4 内", "不允许 SIMD intrinsics", "不允许多线程", "允许展开并重排加法顺序"],
        "examples": [{
            "input": "arr = 100,000 个随机 float",
            "output": "out[0] = 全部元素之和",
            "explanation": "浮点加法不满足严格结合律，因此重排顺序会改变末位结果 —— 本题给出 1e-4 容差，正是为这种重排留出的空间。",
        }],
        "report": {
            "summary": "用 **4 路展开 + 4 个独立累加器** 把一条长度 N 的串行依赖链拆成 4 条长度 N/4 的短链，最后再把 4 个部分和合并。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "`acc += arr[i]` 中每次加法必须等待上一次加法完成 —— 这是一条标准的 RAW 依赖链。",
                    "浮点加法延迟约 4 个周期，因此单累加器循环每 4 个周期才能完成一次加法，IPC 上限被锁死在 0.25 左右。",
                    "注意：本例数据已经是顺序流式访问，缓存不是瓶颈，纯粹是依赖链在限制吞吐。",
                ]},
                {"h": "优化思路", "p": [
                    "引入 acc0..acc3 四个累加器，分别累加下标模 4 的四个相位。",
                    "四条链之间互不相关，乱序核可以在等待 acc0 的加法时发射 acc1、acc2、acc3 的加法。",
                    "理论吞吐提升接近 4 倍（受限于执行单元数量与浮点加法端口数）。",
                ]},
                {"h": "关键改动", "p": [
                    "主循环条件改为 `i <= N - 4`，步长 4，循环体内做四次独立累加。",
                    "尾部不足 4 个元素用第二个简单循环兜底（保证正确性优先于性能）。",
                    "最后以 `(acc0+acc1)+(acc2+acc3)` 的树形结构合并，进一步缩短收尾阶段的依赖链。",
                ]},
                {"h": "为什么更快", "p": [
                    "关键路径长度从 N × 4 周期降到约 (N/4) × 4 周期，IPC 从 ~0.25 提升到接近 1，再叠加循环开销的摊薄可继续上升。",
                    "循环控制指令（比较、自增、跳转）的数量降为 1/4，前端压力同步下降。",
                    "4 是经验值：再多受限于物理寄存器与执行端口，收益会迅速饱和。",
                ]},
                {"h": "常见误区", "p": [
                    "浮点不可随意重排：本题给出 1e-4 容差才允许这样做；若要求逐位一致，则不能改变加法顺序。",
                    "累加器不是越多越好：超过物理寄存器数量会引发寄存器溢出（spill），反而变慢。",
                    "忽略尾部处理：N 不能被展开倍数整除时必须保留收尾循环。",
                ]},
            ],
            "keypoints": ["多累加器拆分依赖链", "树形合并减少收尾延迟", "循环开销摊薄为 1/4"],
            "pitfalls": ["浮点重排受容差约束", "累加器数量受寄存器文件限制"],
        },
    },
    "interleaved-pointer-chasing": {
        "zh": "指针追逐：恢复内存级并行",
        "one_liner": "一条链表是串行的 N 次失效；8 条链表交错遍历，就变成 8 路并行失效。",
        "bottleneck": "内存级并行（MLP）不足",
        "desc": [
            "需要对 8 条独立链表中存储的值求和，每条链表含 125,000 个节点。",
            "基线实现先走完一条链表再走下一条，形成一条很长的串行缓存失效链，CPU 常常空等内存。",
            "请改变遍历顺序，让不同链表的失效能够相互重叠，从而让核心利用更高的内存级并行度。",
        ],
        "prompt": "实现 `long solve(Node **heads, int num_lists)`，每个节点恰好访问一次并返回总和，同时在各链表间交错推进以暴露彼此独立的访存失效。",
        "constraints": ["输入以 8 个链表头指针给出", "每个节点必须恰好处理一次", "不允许多线程", "允许改变跨链表的遍历顺序"],
        "examples": [{
            "input": "heads = 8 个链表头，每链 125,000 个节点",
            "output": "所有节点 val 之和",
            "explanation": "顺序遍历时，下一个节点地址必须等当前节点载入后才能得到 —— 这是最典型的一类串行依赖访存。",
        }],
        "report": {
            "summary": "把\"逐链深度优先\"改成\"跨链广度交错\"：维护 8 个游标，每轮各向前走一步。于是同一时刻有 8 个彼此独立的访存请求在飞行中，MLP 从 1 提升到 8。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "`p = p->next` 意味着下一次访存的地址来自上一次访存的结果 —— 完全串行，无法重叠。",
                    "单条链表遍历时，CPU 每个周期都在等 200 周期的内存延迟，IPC 被压到 0.1 量级。",
                    "这类负载既不受限于带宽，也不受限于计算，纯粹受限于\"并发请求数\"。",
                ]},
                {"h": "优化思路", "p": [
                    "8 条链表彼此独立，遍历第 3 条的节点不需要等待第 1 条的任何结果。",
                    "用 `active[8]` 保存 8 个当前游标，外层 while 每轮对 8 条链各前进一步。",
                    "于是在任意时刻，最多可以有 8 个 cache miss 同时在飞行中，等效延迟降低约 8 倍。",
                ]},
                {"h": "关键改动", "p": [
                    "把嵌套 for 改成 `while (remaining > 0)` + 内层 for 扫描 8 个游标的双层结构。",
                    "每轮重新统计 `remaining`，遇到空指针则跳过，保证所有链表走完后循环自然终止。",
                    "`sum += p->val` 仍是串行加法，但整数加法可以流水执行，不是瓶颈。",
                ]},
                {"h": "为什么更快", "p": [
                    "并发访存数从 1 提升到 8，同样的 200 周期延迟被 8 个请求共享，等效访存延迟大幅下降。",
                    "LLC MPKI 不一定下降（失效次数没变），但**平均失效延迟**显著下降 —— 这正是 MLP 优化的典型特征。",
                    "IPC 提升来自\"不再空等\"，而非指令数减少。",
                ]},
                {"h": "常见误区", "p": [
                    "以为要减少 cache miss：本优化不改变失效次数，只改变失效是否重叠。看指标时应关注平均延迟而非 MPKI。",
                    "交错路数越多越好？超过乱序窗口与 MSHR（失效状态保持寄存器）数量后，多余的请求只会排队，不再带来收益。",
                    "链表长度不一致时，短链走完后会留下空转的游标，需在实现里正确跳过。",
                ]},
            ],
            "keypoints": ["串行失效 → 并行失效", "MLP 是独立于带宽的优化维度", "关注平均失效延迟而非 MPKI"],
            "pitfalls": ["并发度受 MSHR 数量限制", "链表长度不齐时需处理空游标"],
        },
    },
    "bottleneck-triage": {
        "zh": "瓶颈会诊：一次修好三个问题",
        "one_liner": "访存跨行、分支随机、冗余依赖 —— 三处病灶需要同时处理。",
        "bottleneck": "复合瓶颈（分支 + 缓存 + 依赖）",
        "desc": [
            "这个负载之所以慢，有几个相互独立的原因。基线指标：IPC 0.7（目标 > 3.0）、L1 MPKI 36.0（目标 < 4.0）、Branch MPKI 18.0（目标 < 2.0）。",
            "起始代码同时混合了三种瓶颈：局部性差、分支不可预测、以及不必要的依赖压力。",
            "请结合计数器、源码与汇编定位这些问题，并重写循环使三个目标**同时**达成。",
        ],
        "prompt": "重写 `long solve(int *arr, int n)`，返回正确的总和，同时修复基线中的分支、缓存局部性与依赖问题。",
        "constraints": ["输出必须匹配参考总和", "N = 100000，固定种子", "三个目标必须同时达成", "允许 SIMD", "允许改变遍历顺序与控制流"],
        "examples": [{
            "input": "arr = 100,000 个整数，以步长 16 采样",
            "output": "所有满足 `val & 1` 的元素之和",
            "explanation": "基线中的 `sum = sum + (val * 0)` 是人为制造的冗余依赖，实际不改变结果，必须识别并删除。",
        }],
        "report": {
            "summary": "三个病灶逐一清除：①把 `arr[i*16]` 打包成连续数组消除跨行访问；②把 `if (val & 1)` 改写成算术掩码消除分支；③删除 `sum + (val*0)` 冗余依赖并引入 4 个累加器。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "**缓存**：`arr[i * 16]` 的相邻访问间隔 64 字节，每次恰好落在一条新的缓存行上，L1 MPKI 高达 36。",
                    "**分支**：`if (val & 1)` 在随机数据上接近 50/50，Branch MPKI 达 18，每次误预测清空流水线。",
                    "**依赖**：`sum = sum + (val * 0)` 既无意义又在累加器上制造了一次额外的串行依赖，把关键路径硬生生拉长一倍。",
                ]},
                {"h": "优化思路", "p": [
                    "先把要用的数据**打包**到连续数组 `packed[i] = arr[i*16]`：一次 O(n) 的顺序化预处理，把跨行随机访问变成流式扫描。",
                    "把条件累加改写成 `s += (long)(v & 1) * v`：`v & 1` 得到 0 或 1，乘法天然实现了\"条件相加\"，无需任何分支。",
                    "删除 `val * 0`，并用 4 个独立累加器 + 4 路展开打破累加链。",
                ]},
                {"h": "关键改动", "p": [
                    "预处理阶段把稀疏采样结果紧凑存放，后续所有访问都是 stride-1，缓存行利用率 100%。",
                    "热循环中不再有任何条件跳转，Branch MPKI 降到接近 0。",
                    "4 路展开让 4 组「与、乘、加」彼此独立，乱序核可以充分重叠。",
                ]},
                {"h": "为什么更快", "p": [
                    "打包后每条缓存行承载 16 个整数，L1 MPKI 从 36 降到个位数。",
                    "分支消除后不再有流水线清空，前端可以全速供指。",
                    "依赖链被拆成 4 条，IPC 从 0.7 提升到 3 以上。",
                    "三个指标必须一起看：只修其中一项时，另外两项会成为新的瓶颈，目标仍无法达成。",
                ]},
                {"h": "常见误区", "p": [
                    "只优化一处：本题刻意让三个瓶颈相当，任何单点优化都会被剩下的瓶颈顶住。",
                    "忽略 `val * 0` 看似无害：编译器在 `-O0` 或未开启优化时不会消除它，它会真实出现在热循环的关键路径上。",
                    "认为打包需要额外内存就不划算：相对于 100 轮/多次复用，这一次 O(n) 拷贝的收益远大于成本。",
                ]},
            ],
            "keypoints": ["先打包后计算", "掩码乘法替代分支", "删除无意义的依赖"],
            "pitfalls": ["单点优化无法达成复合目标", "必须三项指标同时验证"],
        },
    },
    "strided-sum-prefetch": {
        "zh": "跨步求和：用预取隐藏延迟",
        "one_liner": "每次访问都是一条新缓存行 —— 那就提前把未来 32 行取进来。",
        "bottleneck": "访存延迟串行化",
        "desc": [
            "需要对一个大数组中每 64 字节区域的第一个整数求和。基线读取 `arr[i * 16]`，每次迭代都从一条新的缓存行取数。",
            "如果核心一次只等一个失效，这个循环很容易变成延迟受限。",
            "请使用软件预取、展开或多累加器等手段，让多个访存能够同时在飞行中。",
        ],
        "prompt": "实现 `long solve(int *arr, int n)`，返回 `i = 0..n-1` 上 `arr[i * 16]` 之和，同时重构循环以暴露更多内存级并行。",
        "constraints": ["输入分配空间至少含 n × 16 个整数", "每个 i 必须恰好读取一次 `arr[i * 16]`", "不允许多线程", "不允许 SIMD intrinsics", "允许使用 `__builtin_prefetch`"],
        "examples": [{
            "input": "arr = 大数组，n 次采样，步长 16 个 int（64 字节）",
            "output": "所有采样值之和",
            "explanation": "步长恰好等于一条缓存行，因此每次迭代必然触发一次 L1 失效 —— 硬件 stride 预取器可能帮忙，但不一定可靠。",
        }],
        "report": {
            "summary": "**软件预取 + 4 路展开 + 4 累加器**三管齐下：提前 `PREFETCH_DISTANCE=32` 次迭代发出取指请求，同时把 4 个采样点的加法变成彼此独立的运算。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "步长 16 个 int = 64 字节 = 一条缓存行，因此每次迭代都必然 miss，且只用行内 4 字节。",
                    "基线每轮只发出一个请求，然后干等约 200 周期，吞吐被延迟完全锁死。",
                    "指标上表现为 L2/LLC MPKI 高、IPC 低（约 0.5–1.0）。",
                ]},
                {"h": "优化思路", "p": [
                    "**预取**：在第 i 次迭代发出 `&arr[(i+32)*16]` 的预取请求，等到真正需要时数据已在缓存中。",
                    "**展开**：一次迭代处理 4 个采样点，即同时有 4 个请求在路上。",
                    "**多累加器**：s0..s3 让 4 次加法互不等待，避免加法本身成为新的串行点。",
                ]},
                {"h": "关键改动", "p": [
                    "`if (i + PREFETCH_DISTANCE < n) __builtin_prefetch(...)` —— 边界检查保证不越界预取。",
                    "主循环步长 4，尾部循环处理余数。",
                    "最后 `s0+s1+s2+s3` 合并。",
                ]},
                {"h": "为什么更快", "p": [
                    "预取把\"串行等待\"变成\"提前发起\"：当循环执行到第 i 次时，第 i+32 次的数据已经在路上。",
                    "预取距离需要权衡：太近（如 2–4）来不及掩盖 200 周期延迟；太远（如 256）预取的数据会在用到前被后续失效挤出去。32 是本题实测的经验值。",
                    "注意：本优化不减少失效次数，只让失效在时间上重叠 —— 因此 LLC MPKI 基本不变，而 IPC 明显提升。",
                ]},
                {"h": "常见误区", "p": [
                    "预取距离照抄：最优距离取决于每次迭代的执行时间与内存延迟，换一台机器就要重新实测。",
                    "预取过度：每个元素都预取会让预取指令本身占用发射槽，甚至挤掉有用指令，适得其反。",
                    "以为硬件预取器能兜底：步长 16×4=64 字节恰好在多数硬件预取器的识别范围内，但超过一定跨页边界预取器就会停止工作。",
                ]},
            ],
            "keypoints": ["软件预取提前 32 次迭代", "展开制造并发请求", "预取距离必须实测调参"],
            "pitfalls": ["预取过多占用发射带宽", "跨页时硬件预取失效"],
        },
    },
    "bitset-popcount-throughput": {
        "zh": "位图统计：用 popcount 硬件替代逐位扫描",
        "one_liner": "64 次移位与掩码，被一条 popcnt 指令取代。",
        "bottleneck": "指令数过多 / 未用专用硬件",
        "desc": [
            "需要统计一个大位图中所有被置位的比特数。基线对每个 64 位字用标量内层循环逐位检查 64 个位置。",
            "这种做法浪费了大量指令，也没有用上 CPU 专用的 population count 硬件。",
            "请把逐位扫描替换为吞吐更优、能映射到 popcount 指令的实现。",
        ],
        "prompt": "实现 `unsigned long long solve(unsigned long long *bits, int words)`，返回 `words` 个 64 位字中被置位比特的总数。",
        "constraints": ["words = 65536", "不允许多线程", "允许使用 `__builtin_popcountll` 等编译器内建函数", "结果必须精确"],
        "examples": [{
            "input": "bits = 65536 个 64 位字",
            "output": "全部置位比特数",
            "explanation": "逐位扫描每个字要执行 64 轮「移位 + 与 + 加」，而 popcount 指令一条就能完成同样的工作。",
        }],
        "report": {
            "summary": "用 `__builtin_popcountll` 把每字的 64 次迭代压缩为**一条 popcnt 指令**，再叠加 4 路展开让多条 popcnt 并行执行。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "基线每字执行 64 次 `(word >> b) & 1` 并累加，指令数约为 64 × 4 = 256 条/字。",
                    "这些指令还构成一条累加依赖链，导致即便执行单元空闲也无法提速。",
                    "总指令数约 65536 × 256 ≈ 1677 万条，是纯粹的执行受限（compute-bound）负载。",
                ]},
                {"h": "优化思路", "p": [
                    "现代 x86 提供 `popcnt` 指令，一条指令完成 64 位 population count，编译器内建 `__builtin_popcountll` 会直接映射到它。",
                    "每字指令数从约 256 条降到约 2 条（popcnt + add），理论加速比超过 100 倍。",
                    "再叠加 4 路展开：多条 popcnt 之间没有数据相关，可以并行发射。",
                ]},
                {"h": "关键改动", "p": [
                    "内层 64 次循环整体删除，改为直接调用 `__builtin_popcountll(bits[i])`。",
                    "主循环 4 路展开，四个结果依次累加到 `total`。",
                    "尾部循环处理 `words % 4` 的余数。",
                ]},
                {"h": "为什么更快", "p": [
                    "指令数下降两个数量级，前端不再成为瓶颈，退休指令数大幅减少。",
                    "popcnt 在部分微架构上有 3 周期延迟，但由于各字之间彼此独立，多字并行可以把它完全掩盖。",
                    "IPC 提升到 3.8 以上：此时瓶颈已从\"执行指令\"转移到\"取指与访存\"。",
                ]},
                {"h": "常见误区", "p": [
                    "手写位运算技巧（如 SWAR）未必更快：编译器识别 `__builtin_popcountll` 会直接生成硬件指令，手写版本反而可能阻止这种识别。",
                    "误以为 popcnt 在所有 CPU 上都快：部分老架构上是微码实现，需实测。",
                    "忽略 `-mpopcnt` 编译选项：老编译器可能需要显式开启才能生成该指令，否则会退化为库函数调用。",
                ]},
            ],
            "keypoints": ["用硬件 popcnt 替代逐位循环", "指令数下降两个数量级", "4 路展开掩盖指令延迟"],
            "pitfalls": ["手写 SWAR 可能阻止编译器识别", "需确认目标架构支持 popcnt"],
        },
    },
    "particle-struct-repacking": {
        "zh": "粒子结构体：AoS 转 SoA",
        "one_liner": "把结构体数组拆成并列的字段数组，为展开与向量化铺路。",
        "bottleneck": "带宽与 ILP",
        "desc": [
            "每个粒子以数组-of-structs（AoS）布局存放 4 个 float 字段。计算只用到每个粒子的 `x*w + y*z` 表达式，但基线的处理方式没有把带宽和指令吞吐吃满。",
            "请把热字段重排成更紧凑的布局，或重构循环，让 CPU 更高效地流式处理数据与运算。",
        ],
        "prompt": "实现 `float solve(Particle *p, int n)`，返回全部 n 个粒子的 `sum(p[i].x * p[i].w + p[i].y * p[i].z)`。",
        "constraints": ["n = 200000", "不允许多线程", "允许 SIMD", "不得修改粒子数值"],
        "examples": [{
            "input": "p = 200,000 个 Particle（每个 4 个 float，共 16 字节）",
            "output": "全部粒子的 `x*w + y*z` 之和",
            "explanation": "200,000 × 16B = 3.2MB，已超出常见 L2 容量，因此这是一道典型的带宽受限题目。",
        }],
        "report": {
            "summary": "把 AoS 拆成 4 条独立的 SoA 数组（xs/ys/zs/ws），再用 4 路展开 + 4 累加器让乘法与加法并行执行。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "数据总量 200,000 × 16B = 3.2MB，远超 L1 与多数 L2，因此这是**带宽受限**负载。",
                    "基线每次迭代做 2 次乘、1 次加、1 次累加，而 `total` 的累加链让 4 周期的浮点加法延迟无法被掩盖。",
                    "也就是说：带宽吃不满 + 依赖链拖后腿，两个问题叠加。",
                ]},
                {"h": "优化思路", "p": [
                    "**AoS → SoA**：把 x/y/z/w 拆成四条独立数组。本题四个字段都要用，单看带宽收益有限；真正的价值在于让展开后的访问模式变成四条连续流，编译器更容易生成向量指令。",
                    "**多累加器**：4 个累加器把累加链拆成 4 条，掩盖浮点加法延迟。",
                    "**4 路展开**：每次迭代处理 4 个粒子，摊薄循环开销。",
                ]},
                {"h": "关键改动", "p": [
                    "预处理阶段四趟顺序扫描，把 p[i] 的四个字段分别写入 xs/ys/zs/ws。",
                    "主循环对四个粒子分别用独立累加器计算，最后合并。",
                    "尾部循环处理 n % 4 的余数。",
                ]},
                {"h": "为什么更快 —— 也说说它的局限", "p": [
                    "主要收益来自**多累加器与展开**，而非布局转换本身：这里四个字段全部参与计算，SoA 并不能减少搬运的字节数。",
                    "SoA 的真正价值在两类场景：①只用部分字段时（如只用 x 和 y），可把带宽降到 1/2；②为 SIMD 提供连续同构的数据流。",
                    "工程上更稳的做法是直接用 SIMD intrinsics 对 AoS 做 4 路加载与重排，避免额外的 4 条临时数组（3.2MB 内存开销）。",
                ]},
                {"h": "常见误区", "p": [
                    "以为 AoS→SoA 永远更快：若所有字段都要用且不做向量化，转换的拷贝成本可能大于收益。",
                    "忽略内存开销：本题额外分配 4 × 800KB，在内存紧张场景需权衡。",
                    "忘记释放：参考实现中 malloc 的数组必须 free，否则批量调用会泄漏。",
                ]},
            ],
            "keypoints": ["AoS → SoA 为向量化铺路", "4 累加器掩盖浮点延迟", "诚实看待布局转换的真实收益"],
            "pitfalls": ["全字段使用时收益有限", "额外内存分配需权衡"],
        },
    },
    "image-blur-tiling": {
        "zh": "图像模糊：让工作集留在缓存里",
        "one_liner": "水平 3-tap 卷积 —— 收益主要来自指针外提与展开。",
        "bottleneck": "缓存行复用与循环开销",
        "desc": [
            "对 512×512 图像做水平 3-tap 模糊。每像素的算术量很小，因此性能主要取决于能否让行数据保持缓存热、并按顺序写出结果。",
            "请用指针运算、分块、展开或类似的循环改进重写起始代码，使工作集保持缓存友好。",
        ],
        "prompt": "实现 `void solve(float *in, float *out, int width, int height)`，使每个非边缘输出像素等于其左、中、右三邻居的平均，边缘像素原样复制。",
        "constraints": ["width = height = 512", "边缘像素必须原样复制", "不允许多线程", "允许 SIMD"],
        "examples": [{
            "input": "512×512 浮点图像",
            "output": "水平 3-tap 模糊后的图像，边缘保持不变",
            "explanation": "单张图像 512×512×4B = 1MB，超过 L1 但能进 L2；因此按行分块能让输入行在 L1 中复用。",
        }],
        "report": {
            "summary": "按行分块（32 行一块）+ 行指针外提 + 4 路展开。但要诚实说明：水平 1D 卷积本身已是顺序访问，**真正的收益来自展开与指针优化**，分块在这里只是次要贡献。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "基线已经是行优先顺序访问，空间局部性本身不差。",
                    "真正的浪费在两处：①每次迭代都要重算 `row + x` 与三次数组下标；②每次迭代只有一次乘加，循环控制指令占比过高。",
                    "图像共 1MB，超出 L1（32KB），因此仍存在行被反复从 L2 读取的压力。",
                ]},
                {"h": "优化思路", "p": [
                    "**指针外提**：把 `in + row`、`out + row` 提到行循环外，内层直接用 `src[x]`、`dst[x]`，消除重复的地址计算。",
                    "**4 路展开**：一次迭代写 4 个输出像素，摊薄比较与跳转开销，同时让 4 组乘加彼此独立。",
                    "**按行分块**：以 32 行为一块遍历，让输入行在块内保持缓存热（对二维 stencil 收益更明显）。",
                ]},
                {"h": "关键改动", "p": [
                    "外层 `y0` 以 `TILE_ROWS=32` 步进，内层遍历块内各行。",
                    "行首 `dst[0] = src[0]`、行尾 `dst[width-1] = src[width-1]` 单独处理，中间部分可安全展开。",
                    "主循环条件 `x <= width - 5` 保证展开时不越界，余数由收尾循环处理。",
                ]},
                {"h": "为什么更快 —— 也说说局限", "p": [
                    "指针外提减少了约 3 次整数加法/迭代；展开把循环控制指令降为 1/4。",
                    "乘加之间彼此独立，乱序核可以重叠执行，IPC 提升明显。",
                    "必须承认：对纯水平 stencil，分块的贡献有限 —— 因为每行只被读一次（3 个邻居在行内，不跨行复用）。分块的真正舞台是第 11 题那种跨行的 2D 五点 stencil。",
                ]},
                {"h": "常见误区", "p": [
                    "盲目加分块：若数据没有跨块复用，分块只会增加代码复杂度。",
                    "展开越宽越好：过宽会导致寄存器溢出并撑大热循环，反而伤害指令缓存。",
                    "忽略边缘：展开时必须保证 `x+4` 不越界，否则会读到下一行的开头。",
                ]},
            ],
            "keypoints": ["指针外提消除重复地址计算", "4 路展开摊薄循环开销", "分块对水平 stencil 收益有限"],
            "pitfalls": ["展开过宽引发寄存器溢出", "边缘像素需单独处理"],
        },
    },
    "branchless-score-window": {
        "zh": "区间计分：用无符号技巧消除分支",
        "one_liner": "一次比较同时判断上下界 —— 负数回绕成巨大的无符号数。",
        "bottleneck": "不可预测分支",
        "desc": [
            "一个计分循环只累加落在目标区间内的值。基线对每个元素使用两层嵌套分支。",
            "当输入分数大致均匀分布时，这些分支变得难以预测，损害前端吞吐。",
            "请重写循环，在保持结果精确的前提下让热路径变得高度可预测，最好彻底无分支。",
        ],
        "prompt": "实现 `long solve(int *scores, int n)`，返回所有落在闭区间 [50, 200] 内的 `scores[i]` 之和。",
        "constraints": ["scores[i] 为 0..255 范围内的整数", "不允许多线程", "允许 SIMD", "结果必须精确"],
        "examples": [{
            "input": "scores = 均匀分布在 0..255 的整数数组",
            "output": "所有 [50, 200] 区间内元素之和",
            "explanation": "利用无符号回绕：`(unsigned)(s - 50)` 在 s < 50 时会变成一个巨大的无符号数，因此一次 `<= 150U` 就能同时排除下界与上界。",
        }],
        "report": {
            "summary": "把两层嵌套 if 压缩为**一次无符号比较**：`(unsigned)(scores[i] - 50) <= 150U`，再用布尔值转整数做乘法完成条件累加。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "基线对每个元素做两次判断：`s >= 50` 与 `s <= 200`，分支数翻倍。",
                    "分数在 0..255 均匀分布时，落在 [50,200] 内的概率约 59%，处于最难预测的概率区间。",
                    "更糟的是，分支还阻止了编译器对该循环做向量化。",
                ]},
                {"h": "优化思路", "p": [
                    "经典的无符号区间技巧：要判断 `lo <= x <= hi`，只需计算 `(unsigned)(x - lo) <= (unsigned)(hi - lo)`。",
                    "原理：当 `x < lo` 时，`x - lo` 为负数，转成无符号后会回绕成一个巨大的值，自然被 `<=` 判否。",
                    "把布尔结果 `(window <= 150U)` 直接当作 0/1 参与乘法：`total += (long)(cond) * scores[i]`。",
                ]},
                {"h": "关键改动", "p": [
                    "`unsigned int window = (unsigned int)(scores[i] - 50);` —— 关键就在强制转成无符号。",
                    "`total += (long)(window <= 150U) * scores[i];` —— 比较结果 0/1 直接作为乘数。",
                    "循环体内不再有任何跳转指令。",
                ]},
                {"h": "为什么更快", "p": [
                    "Branch MPKI 降到接近 0，不再有流水线清空。",
                    "无分支的直线代码让编译器可以安全向量化，一次处理多个元素。",
                    "代价是无论是否命中区间都要做一次乘法 —— 当分支高度可预测时（例如 99% 命中），这种写法反而可能更慢。",
                ]},
                {"h": "常见误区", "p": [
                    "忘记无符号转换：若 `window` 声明为 int，负数不会回绕，判断就错了。",
                    "溢出风险：`x - lo` 本身在有符号域的溢出是未定义行为，务必先确认 x、lo 的取值范围。",
                    "认为无分支永远更快：无分支把\"条件执行\"变成\"总是执行\"，只有当分支误预测代价高于多做的算术时才划算。",
                ]},
            ],
            "keypoints": ["无符号回绕实现一次比较定区间", "布尔转整数做条件累加", "为向量化创造条件"],
            "pitfalls": ["必须确保无符号转换", "分支可预测时反而可能变慢"],
        },
    },
    "ece-jacobi-stencil-lab": {
        "zh": "Jacobi 五点 stencil：二维分块",
        "one_liner": "1024×1024 网格 = 4MB —— 按 32 行分块，让三行数据留在 L1。",
        "bottleneck": "跨行访问的缓存复用",
        "desc": [
            "本题复现了计算机体系结构高年级课程中经典的缓存优化实验：对 1024×1024 网格做一次 Jacobi 迭代。",
            "基线结果正确，但它在热循环中流式扫过巨大的二维数组，几乎没有考虑缓存复用。",
            "请重写内核，在保持数值结果一致的前提下改善 L1 与 L2 中的局部性。行分块、指针外提等都是可行手段。",
        ],
        "prompt": "实现 `void solve(const float *in, float *out, int n)`，在 n×n 行优先网格上完成一次五点 Jacobi 更新。边界单元原样复制，内部单元设为 `0.5*center + 0.125*(north + south + west + east)`。",
        "constraints": ["评测中 n = 1024", "边界单元必须与输入逐位一致", "内部更新公式必须保持数值等价", "不允许多线程", "允许 SIMD（非必需）"],
        "examples": [{
            "input": "1024×1024 行优先 float 网格",
            "output": "一次 Jacobi 迭代后的网格",
            "explanation": "每个内部单元要读 5 个值：本行左右 + 上下两行同列。若按行扫描且不复用，每行数据会被从内存读入三次。",
        }],
        "report": {
            "summary": "按 32 行分块，块内为每行维护 north/center/south 三个行指针，并做 4 路展开。这样三行的工作集约 12KB，稳定留在 L1 中，上下行在相邻行之间被复用。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "网格 1024×1024×4B = 4MB，远超 L2；朴素按行扫描时，每一行会被读取三次（作为 north、center、south）。",
                    "若三次读取之间该行已被挤出缓存，等效访存量就是 12MB 而非 4MB。",
                    "此外每次迭代要计算 5 个地址（`idx-n`、`idx+n`、`idx-1`、`idx+1`、`idx`），整数运算开销显著。",
                ]},
                {"h": "优化思路", "p": [
                    "**行分块**：以 `TILE_ROWS=32` 为一块，块内连续处理 32 行。此时活跃数据只有 3 行 × 4KB = 12KB，轻松驻留 L1。",
                    "**三指针外提**：在行循环内计算一次 `north/center/south` 行首指针，内层 x 循环只做 `center[x]` 这样的偏移访问。",
                    "**4 路展开**：一次迭代算 4 个输出单元，5 个输入值的乘加彼此独立。",
                ]},
                {"h": "关键改动", "p": [
                    "先把首行与末行整体复制（边界），内部区域单独处理，避免在热循环里做 `if (y==0 || y==n-1)` 判断。",
                    "行循环内只做一次乘法得到行首地址，把 `y*n` 从内层循环彻底消除。",
                    "展开时 `x <= n-5` 保证 `center[x+4]` 不越到下一行。",
                ]},
                {"h": "为什么更快", "p": [
                    "上下行复用：处理第 y 行时的 north 就是处理第 y-1 行时的 center，已经在 L1 里，省去一次内存读取。",
                    "等效访存量从 12MB 降到接近 4MB，L1 MPKI 与 LLC MPKI 同时下降。",
                    "指针外提与展开把每单元的地址计算从 5 次降到约 0.25 次。",
                ]},
                {"h": "常见误区", "p": [
                    "分块行数随意取：太小则块间切换开销大，太大则三行工作集超出 L1；32 行对应约 12KB，是 32KB L1 下的稳妥取值。",
                    "时间分块（沿时间维做多轮迭代的分块）在只做一次迭代时无效 —— 本题只迭代一次，因此只能做空间分块。",
                    "边界判断放进热循环：应像参考实现那样把边界整体外提处理。",
                ]},
            ],
            "keypoints": ["32 行空间分块", "north/center/south 三指针外提", "边界外提 + 4 路展开"],
            "pitfalls": ["分块行数需匹配 L1 容量", "只迭代一次时时间分块无效"],
        },
    },
}
