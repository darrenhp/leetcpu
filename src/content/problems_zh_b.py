# -*- coding: utf-8 -*-
"""第 12–22 题的中文题面与优化报告。"""

PROBLEMS_ZH = {
    "irregular-gather-prefetch": {
        "zh": "不规则聚集：为随机访问提前预取",
        "one_liner": "索引流是顺序的、数据流是随机的 —— 那就用前者去预取后者。",
        "bottleneck": "随机访存延迟串行化",
        "desc": [
            "需要按一个索引置换定义的随机顺序，对 128K 个整数求和。基线顺序遍历 `indices[]`，并按需取 `data[indices[i]]`。",
            "由于每个索引随机指向 512KB 数据数组中的某个位置，每次迭代都会触发一次 L1 失效 —— CPU 在算出下一个地址之前只能干等。",
            "请加入软件预取提示，让 CPU 提前为后面的迭代发起数据请求。",
        ],
        "prompt": "实现 `long solve(const int *data, const int *indices, int n)`，返回所有 `data[indices[i]]` 之和，并使用 `__builtin_prefetch` 隐藏随机访存延迟。",
        "constraints": ["n = 131072（128K，固定）", "indices[] 是 0..n-1 的一个置换 —— 每个元素恰好访问一次", "允许且需要使用 `__builtin_prefetch` 才能达标", "不允许多线程", "不允许 SIMD intrinsics", "结果必须精确"],
        "examples": [{
            "input": "data = 512KB int 数组；indices = 128K 个随机下标",
            "output": "所有被引用元素之和",
            "explanation": "关键洞察：`indices[]` 本身是顺序访问的、缓存友好的，因此可以用它提前知道未来要访问哪些 data 地址。",
        }],
        "report": {
            "summary": "利用**索引流的顺序性**为数据流做预取：在第 i 次迭代就读取 `indices[i+24]`（顺序、几乎必然命中缓存），并据此预取 `data[indices[i+24]]`，把串行等待变成提前发起。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "`sum += data[indices[i]]` 中，数据地址依赖 `indices[i]`，而 `indices[i]` 本身是顺序读的（缓存友好）；真正的失效全部发生在 `data` 侧。",
                    "每次数据访问都是一次串行 L1 失效：必须等当前值返回、累加完成后，下一次迭代才能发起新请求。",
                    "指标特征：L1D 平均失效延迟极高（可达数十周期），IPC 很低。",
                ]},
                {"h": "优化思路", "p": [
                    "随机访问是硬件 stride 预取器的盲区，只能靠软件预取。",
                    "由于 `indices` 是顺序的，读取 `indices[i + D]` 几乎不会引入额外失效 —— 这是一笔\"免费\"的先知信息。",
                    "提前 D 次迭代发起预取，使得真正需要该数据时它已在缓存中；同时保持 D 个请求在飞行中。",
                ]},
                {"h": "关键改动", "p": [
                    "`if (i + PREFETCH_DIST < n) __builtin_prefetch(&data[indices[i + PREFETCH_DIST]], 0, 1);`",
                    "参数含义：`0` 表示读预取，`1` 表示高时间局部性（尽量留在各级缓存）。",
                    "本题 `PREFETCH_DIST = 24`。",
                ]},
                {"h": "为什么更快", "p": [
                    "任意时刻有约 24 个数据请求在飞行中，把 200 周期的延迟摊薄到 24 个请求上。",
                    "代价是额外的索引读取，但 `indices` 顺序访问、缓存命中率接近 100%，几乎不增加访存压力。",
                    "预取距离需要匹配\"每次迭代耗时 × 内存延迟\"：D 太小来不及，D 太大预取的数据会在用到前被淘汰。",
                ]},
                {"h": "常见误区", "p": [
                    "预取距离照抄 24：换成更大数组或更慢内存后，最优值会变化，必须实测扫描。",
                    "忘记边界检查：`i + D` 越界会读取 `indices` 尾部之外，产生越界访问。",
                    "以为预取能减少失效次数：它只让失效重叠，MPKI 不变，变的是平均失效延迟。",
                ]},
            ],
            "keypoints": ["用顺序的索引流预取随机的数据流", "预取距离需实测", "关注平均失效延迟而非 MPKI"],
            "pitfalls": ["越界访问风险", "预取距离与环境强相关"],
        },
    },
    "histogram-dependency-chains": {
        "zh": "直方图：打断写依赖链",
        "one_liner": "相邻元素撞进同一个桶时会串行 —— 用 4 份部分直方图隔开它们。",
        "bottleneck": "写后写（WAW）依赖",
        "desc": [
            "用 200 万个随机字节填充 256 桶的直方图。基线对每个元素执行 `hist[arr[i]]++`。",
            "当连续两个元素落在同一个桶时，第二次自增必须等第一次完成 —— 这是一条写后写依赖链，会让流水线停顿。",
            "请并行计算若干份彼此独立的局部直方图，最后再合并，从而打断这些链。",
        ],
        "prompt": "实现 `void solve(const unsigned char *arr, int n, unsigned int *hist)`，统计每个字节值 0–255 的出现次数（hist[] 进入时已清零），使用交错的局部直方图减少写依赖停顿。",
        "constraints": ["n = 2,000,000（固定）", "调用前 hist[] 已清零", "hist[i] 必须与参考计数完全一致", "不允许多线程", "允许 SIMD"],
        "examples": [{
            "input": "arr = 2,000,000 个 0–255 的随机字节",
            "output": "256 个桶的精确计数",
            "explanation": "桶只有 256 个（1KB），随机数据下相邻元素撞桶概率约 1/256，看似不高，但在 200 万次迭代中会造成大量零星的串行停顿。",
        }],
        "report": {
            "summary": "维护 **4 份独立的 256 桶局部直方图**，让相邻 4 个元素分别写入不同数组，从而消除写后写冲突；最后把 4 份结果逐桶相加。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "`hist[arr[i]]++` 是一次读-改-写。当 `arr[i] == arr[i+1]` 时，第二次自增必须等第一次的写完成 —— 这就是写后写（WAW）相关。",
                    "无序执行无法绕过它：乱序核必须保证对同一地址的访问维持程序顺序。",
                    "单份 1KB 的直方图常驻 L1，带宽不是问题；瓶颈纯粹是这些零星但频繁的依赖停顿。",
                ]},
                {"h": "优化思路", "p": [
                    "引入 h0/h1/h2/h3 四份直方图，第 i 个元素按 `i % 4` 写入对应的一份。",
                    "相邻 4 个元素必然写入不同数组，彼此之间没有地址冲突，可以完全并行。",
                    "只在相隔 4 个元素之间才可能撞桶，冲突概率从 1/256 降到约 1/256⁴ 量级（对同一份数组而言间隔被拉开 4 倍）。",
                ]},
                {"h": "关键改动", "p": [
                    "4 路展开，四行 `hX[arr[i+k]]++` 写在同一个循环体内。",
                    "四份数组各 1KB，合计 4KB，仍然稳稳驻留 L1。",
                    "收尾阶段 `hist[j] = h0[j]+h1[j]+h2[j]+h3[j]`，只有 256 次，成本可忽略。",
                ]},
                {"h": "为什么更快", "p": [
                    "写依赖链被打断，自增操作可以流水执行，IPC 明显提升。",
                    "L1D 平均延迟下降：不再需要等待前一次对同一地址的写完成。",
                    "代价是 4KB 栈空间与 256 次合并 —— 相对于 200 万次迭代可以忽略。",
                ]},
                {"h": "常见误区", "p": [
                    "份数越多越好？每份 1KB，8 份就是 8KB，再乘以更宽的展开会开始挤占 L1；通常 4 份是甜点。",
                    "忘记清零：四份局部直方图若声明在栈上必须用 `= {0}` 初始化，否则结果全错。",
                    "忽略溢出：200 万 / 256 ≈ 7812，unsigned int 足够；但若数据规模更大需换用更宽的类型。",
                ]},
            ],
            "keypoints": ["多份局部直方图消除 WAW 冲突", "总容量仍驻留 L1", "合并成本可忽略"],
            "pitfalls": ["份数过多会挤占 L1", "必须显式初始化为 0"],
        },
    },
    "3d-loop-interchange": {
        "zh": "三维数组：修正循环顺序",
        "one_liner": "把最快变化的一维放到最内层 —— 步長从 16KB 降到 4 字节。",
        "bottleneck": "循环顺序导致的跨步访问",
        "desc": [
            "对 64×64×64 的整数数组求和。数组按行优先存储：元素 `[i][j][k]` 位于偏移 `i*64*64 + j*64 + k`。",
            "当 k 在最内层变化时，相邻访问在内存中彼此相邻 —— 一条缓存行可覆盖 16 个整数。",
            "基线却把 i 放在最内层，于是每步跳跃 64×64 = 4096 个整数（16KB），每次访问都要拉一条新的缓存行。",
        ],
        "prompt": "实现 `long solve(int *arr, int n)`，返回以行优先存于 `arr` 的 n×n×n 个整数之和，且最内层循环遍历最后一个维度 k。",
        "constraints": ["n = 64（固定，数组为 n×n×n = 262144 个 int）", "arr 行优先：元素 [i][j][k] 位于 arr[i*n*n + j*n + k]", "不允许多线程", "结果必须精确"],
        "examples": [{
            "input": "64×64×64 行优先 int 数组（共 1MB）",
            "output": "全部元素之和",
            "explanation": "仅交换循环顺序即可：数组总量 1MB 虽超出 L1，但顺序扫描能 100% 利用每条缓存行，且硬件预取器可以完美工作。",
        }],
        "report": {
            "summary": "纯循环交换：把 `for k / for j / for i` 改成 `for i / for j / for k`，让最内层沿内存中连续的方向推进。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "基线最内层 `sum += arr[i*n*n + j*n + k]`，i 变化时地址跳 `n*n*4 = 16384` 字节。",
                    "整个数组 1MB，远大于 L1；跨步 16KB 意味着每条缓存行只用 4 字节，有效率 1/16。",
                    "更糟的是：16KB 的固定跨步超出多数硬件预取器的识别范围，预取器完全帮不上忙。",
                ]},
                {"h": "优化思路", "p": [
                    "行优先数组中，最后一维（k）在内存中连续。让 k 成为最内层循环变量，相邻迭代地址只差 4 字节。",
                    "这样每条 64 字节缓存行被连续使用 16 次，利用率 100%。",
                    "顺序流式访问还能被硬件 stride 预取器完美预测，进一步掩盖剩余延迟。",
                ]},
                {"h": "关键改动", "p": [
                    "三重循环顺序由 k→j→i 改为 i→j→k（循环体完全不变）。",
                    "求和满足交换律，因此改变遍历顺序不影响结果。",
                    "索引表达式 `i*n*n + j*n + k` 保持不变 —— 编译后内层实际是 `base++` 的顺序扫描。",
                ]},
                {"h": "为什么更快", "p": [
                    "缓存行利用率从 4/64 提升到 64/64，等效访存量降为 1/16。",
                    "L1 MPKI 从数十降到个位数，硬件预取器开始生效。",
                    "这是最经典的\"零成本优化\"：不改数据结构、不改算法，只调整循环嵌套顺序。",
                ]},
                {"h": "常见误区", "p": [
                    "以为编译器会自动做循环交换：C 编译器通常不敢擅自改变浮点求和顺序；对整数虽可能，但依赖别名分析，不能指望。",
                    "混淆行优先与列优先：Fortran/NumPy 默认列优先，此时最快变化的是**第一维**，方向恰好相反。",
                    "忽略 TLB：跨步 16KB 也会在页面之间跳跃，进一步加剧 TLB 压力。",
                ]},
            ],
            "keypoints": ["循环交换让内层连续访问", "缓存行利用率提升 16 倍", "零成本改动"],
            "pitfalls": ["行/列优先语言差异", "不要指望编译器自动交换"],
        },
    },
    "binary-search-eytzinger": {
        "zh": "二分查找：Eytzinger 布局 + 预取",
        "one_liner": "把 O(log N) 次串行失效，变成按层批发的并行失效。",
        "bottleneck": "串行依赖的缓存失效",
        "desc": [
            "在含 65,536 个键的有序数组中查找 100,000 个查询值。标准二分查找每次查询要触碰 O(log₂N) ≈ 16 条缓存行，每层一条。",
            "每次失效都是串行的 —— 在当前的比较得出结果之前，下一个地址是未知的。",
            "10 万次查询就是约 160 万次串行依赖的缓存失效。Eytzinger 布局以 BFS 顺序存储查找树：根在下标 1，其子节点在 2 和 3……",
        ],
        "prompt": "实现 `int solve(const int *keys, int n, const int *queries, int q)`，返回在键集合中被命中的查询数量。允许在查找前内部重排 keys。",
        "constraints": ["keys[] 升序且含 n 个互异值（≤65536）", "q = 100000 次查询", "返回精确的命中计数", "不允许多线程", "允许并鼓励使用 `__builtin_prefetch`"],
        "examples": [{
            "input": "keys = 65,536 个有序 int；queries = 100,000 个查询值",
            "output": "命中的查询个数",
            "explanation": "Eytzinger 布局中第 k 层的节点下标固定在 [2^k, 2^(k+1))，因此可以在处理第 k 层时一次性预取第 k+2、k+3 层的整段节点。",
        }],
        "report": {
            "summary": "两招并用：①把数组重排为 **Eytzinger（BFS）布局**，使查找可以用 `k = 2k + (eyt[k] < x)` 无分支推进；②在处理第 k 层时用 `__builtin_prefetch(&eyt[4*k])` 一次性把未来若干层的节点批量取入缓存。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "标准二分查找的每一步地址都依赖上一步的比较结果 —— 16 次完全串行的缓存失效。",
                    "10 万次查询 × 16 层 ≈ 160 万次串行失效，每次约 200 周期，总计数亿周期的纯等待。",
                    "此外 `lo`/`hi` 两条分支各自都难以预测，进一步加剧前端开销。",
                ]},
                {"h": "优化思路", "p": [
                    "**Eytzinger 布局**：把有序数组按\"二分查找树\"的 BFS 顺序重排，根在下标 1，左右孩子在 `2k` 与 `2k+1`。",
                    "于是查找可以写成无分支形式：`k = 2*k + (eyt[k] < target)` —— 只需一条 setl/比较指令，没有跳转。",
                    "**层间预取**：第 k 层的节点下标都落在 `[2^k, 2^(k+1))`，而 `4*k` 恰好指向第 k+2 层的区域。在处理第 k 层时预取 `eyt[4*k]`，就能提前两层发起请求，让多层失效重叠。",
                ]},
                {"h": "关键改动", "p": [
                    "递归 `build()` 以中序遍历有序数组，把中位数放在 `eyt[node]`，左右半区递归到 `2*node` 与 `2*node+1`。",
                    "查找循环：`while (k <= n) { __builtin_prefetch(&eyt[4*k], 0, 0); if (eyt[k] == target) {...} k = 2*k + (eyt[k] < target); }`",
                    "用 `calloc(n+2, ...)` 分配，保证越界位置为 0，循环条件 `k <= n` 即可自然终止。",
                ]},
                {"h": "为什么更快", "p": [
                    "无分支推进消除了分支预测失败，也让查找路径不再因误预测而被清空。",
                    "层间预取让相距两层的节点并行取入，把 16 次串行失效压缩成约 16/2 次批量失效。",
                    "BFS 布局的额外好处：树的顶部几层体积极小（前 4 层只有 15 个节点），几乎永远驻留 L1，因此前几次比较基本不失效。",
                ]},
                {"h": "常见误区", "p": [
                    "只做 Eytzinger 不做预取：无分支推进能减少前端开销，但串行失效依然存在，收益有限。",
                    "预取 `2*k` 而非 `4*k`：预取下一层来不及掩盖 200 周期延迟，必须跳两层以上。",
                    "忽略构建成本：重排是 O(n)，只有在查询次数远大于键数量时才划算（本题 10 万查询 vs 6.5 万键，处于临界，实际收益依赖查询量）。",
                ]},
            ],
            "keypoints": ["BFS 布局支持无分支推进", "按层批发预取打破串行", "树顶层天然驻留 L1"],
            "pitfalls": ["只改布局不预取收益有限", "构建成本需被查询量摊薄"],
        },
    },
    "streaming-square-sum": {
        "zh": "流式平方和：多累加器提升 ILP",
        "one_liner": "一个累加器锁死在每周期一次加法 —— 用 4 个把它拆开。",
        "bottleneck": "累加依赖链",
        "desc": [
            "对 100 万个字节计算 `sum((src[i] * src[i]) & 0xFFFF)`。基线使用单个累加器，于是每次加法都必须等上一次完成。",
            "这条串行依赖链把 CPU 限制在每周期一次加法，无论执行单元有多宽。",
            "请展开循环并使用多个彼此独立的累加器，让乱序核能同时发射多次乘-掩-加。",
        ],
        "prompt": "实现 `long solve(const unsigned char *src, int n)`，返回 `sum((src[i] * src[i]) & 0xFFFF)`，使用多个独立累加器以挖掘指令级并行。",
        "constraints": ["n = 1,000,000（固定）", "src[i] 为 1–256 的字节值", "每元素计算：`(src[i] * src[i]) & 0xFFFF`", "结果必须精确", "不允许多线程", "允许 SIMD"],
        "examples": [{
            "input": "src = 1,000,000 个字节",
            "output": "平方值低 16 位之和",
            "explanation": "乘法与掩码可以完全并行，唯一串行的是累加 —— 因此拆累加器就是本题的全部答案。",
        }],
        "report": {
            "summary": "4 路展开 + 4 个独立累加器：把一条长度 100 万的加法链拆成 4 条长度 25 万的短链，最后合并。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "`sum += ...` 中每次加法依赖上一次结果，形成标准 RAW 链。整数加法虽只有 1 周期延迟，但这已经把 IPC 锁死在 1 附近。",
                    "实际的每元素工作量是「乘法 + 与 + 加法」三条指令，理论上可以接近每周期 3 条 → IPC 目标 3.5 意味着必须打破这条链。",
                    "数据只有 1MB 且完全顺序访问，缓存不是瓶颈。",
                ]},
                {"h": "优化思路", "p": [
                    "引入 s0..s3，分别累加下标模 4 的四个相位。",
                    "四组「乘法-掩码-加法」之间没有数据相关，乱序核可以连续发射。",
                    "同时把循环控制指令（比较、自增、跳转）摊薄到 1/4。",
                ]},
                {"h": "关键改动", "p": [
                    "主循环 `for (; i <= n - 4; i += 4)`，循环体内四行独立累加。",
                    "注意保留 `(unsigned int)` 转换：`src[i]` 是 unsigned char，若不提升就相乘会溢出。",
                    "尾部循环处理余数，最后 `s0+s1+s2+s3`。",
                ]},
                {"h": "为什么更快", "p": [
                    "关键路径缩短为 1/4，IPC 从约 1.0 提升到 3.5 以上。",
                    "乘法与与运算本来就可以并行，此前只是被累加链拖住；拆链后它们的并行度才真正释放出来。",
                    "这是纯 ILP 优化：指令总数几乎不变，只是执行顺序变了。",
                ]},
                {"h": "常见误区", "p": [
                    "忘记整型提升：`(unsigned char)200 * 200 = 40000` 会溢出 signed char/char 的运算结果，必须先转 unsigned int。",
                    "累加器过多：超过可用寄存器会引发 spill，反而变慢；4–8 个通常是甜点。",
                    "整数求和可安全重排，但浮点不行 —— 换成 float 就要考虑精度。",
                ]},
            ],
            "keypoints": ["多累加器拆依赖链", "循环开销摊薄为 1/4", "释放乘法与掩码的并行度"],
            "pitfalls": ["整型提升与溢出", "累加器数量受寄存器限制"],
        },
    },
    "row-major-cache-locality": {
        "zh": "二维网格求和：修正列优先遍历",
        "one_liner": "步长 8KB 的访问模式 —— 改成行优先，再加 64×64 分块。",
        "bottleneck": "列优先跨步访问",
        "desc": [
            "对一个大型行优先二维网格中的所有值求和。基线按列优先遍历：外层沿列（x）推进，内层沿行（y）下移。",
            "由于数组按行优先存储，内层循环的相邻迭代会跳过整整一行的宽度 —— 2048 个整数，即 8KB。",
            "每一步都落在一条冷缓存行上，于是内层循环几乎总在等待内存。",
        ],
        "prompt": "实现 `long solve(int *grid, int n)`，返回 n×n 行优先整数网格的精确和，并以缓存友好的顺序遍历内存。",
        "constraints": ["n = 2048（评测中固定）", "grid 行优先存储：元素 [y][x] 位于 grid[y*n + x]", "结果必须精确", "不允许多线程", "允许 SIMD"],
        "examples": [{
            "input": "2048×2048 行优先 int 网格（16MB）",
            "output": "全部元素之和",
            "explanation": "每次跨 8KB 跳跃还会跨页，因此 TLB 压力同样很大；改成行优先后两个问题一起解决。",
        }],
        "report": {
            "summary": "先**交换循环顺序**让内层沿行连续推进，再叠加 **64×64 分块**，使一次处理的行段工作集稳定落在 L1 中。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "内层 `sum += grid[y*n + x]`，y 变化时地址跳 2048×4 = 8192 字节。",
                    "整个网格 16MB，远超 L2/LLC 可容纳范围；每次访问拉入一条 64 字节缓存行却只用 4 字节。",
                    "8KB 的跨步同时跨越页边界，导致 dTLB 也持续未命中。",
                ]},
                {"h": "优化思路", "p": [
                    "**循环交换**：把 x 循环放内层，使相邻访问地址只差 4 字节，缓存行利用率 100%。",
                    "**分块**：即使行优先，一次扫完整行（2048×4 = 8KB）后这些数据也不会再被用到，无复用可言；但分块能让 64×64 的块（16KB）在处理期间驻留 L1，配合多累加器可获得更好的流式吞吐。",
                    "行指针外提：`int *row = grid + y*n` 提到行循环外，内层只做 `row[x]`。",
                ]},
                {"h": "关键改动", "p": [
                    "外层 `y0` 与 `x0` 均以 `TILE=64` 步进，内层按行优先顺序遍历块内元素。",
                    "用 `y1/x1` 处理不能整除的尾块。",
                    "行首指针 `row` 在行循环内计算一次。",
                ]},
                {"h": "为什么更快", "p": [
                    "循环交换是主要收益：缓存行利用率从 1/16 提升到 100%，等效访存量降为 1/16，且硬件预取器开始生效。",
                    "分块在这里是次要收益 —— 纯求和没有数据复用，分块主要作用是让访问在物理页上更聚集、减轻 TLB 压力。",
                    "诚实地说：对一次性全量求和，仅做循环交换就已解决绝大部分问题，分块属于锦上添花。",
                ]},
                {"h": "常见误区", "p": [
                    "以为分块必然带来巨大提升：没有数据复用的场景（如本题的纯求和）中，分块收益远小于 stencil/矩阵乘。",
                    "忽略 TLB：16MB 工作集在 4KB 页下需要 4096 个页表项，远超典型 L1 dTLB 容量，这往往是残留瓶颈。",
                    "块大小拍脑袋：64×64 ints = 16KB，是 32KB L1 下的稳妥选择；换成 128 就会溢出。",
                ]},
            ],
            "keypoints": ["循环交换是核心收益", "行指针外提", "分块主要缓解 TLB 压力"],
            "pitfalls": ["纯求和无复用，分块收益有限", "大工作集下 TLB 是残留瓶颈"],
        },
    },
    "saxpy-auto-vectorize": {
        "zh": "SAXPY：解锁自动向量化",
        "one_liner": "把循环写成规整形式，让编译器放心生成 SIMD。",
        "bottleneck": "未向量化 / 循环开销",
        "desc": [
            "在大规模 float 数组上计算经典的 SAXPY 更新 `y[i] = a*x[i] + y[i]`。基线结果正确但刻意写得保守。",
            "请把循环重写成规整、对向量化友好的形式，让编译器与乱序核能榨出更多吞吐。",
            "这是从 ILP 思维过渡到 SIMD 风格优化的实用桥梁 —— 即便不写 intrinsics。",
        ],
        "prompt": "实现 `void solve(float a, const float *x, float *y, int n)`，用 `a*x[i] + y[i]` 更新 y 的全部 n 个元素。",
        "constraints": ["n = 262144（评测中固定）", "不允许多线程", "不允许外部库", "不要求 SIMD intrinsics", "浮点校验和容差：1e-4"],
        "examples": [{
            "input": "x、y 为 262,144 个 float；a 为标量",
            "output": "更新后的 y",
            "explanation": "每个元素只做一次乘加，是典型的访存受限负载；向量化的收益主要来自减少指令数与循环开销。",
        }],
        "report": {
            "summary": "把循环改写成**无临时变量、步长为 8 的规整展开形式**，消除一切让编译器犹豫的因素（别名、复杂控制流、逐元素临时量），从而触发自动向量化。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "每元素只做 1 次乘 + 1 次加 + 2 次访存，是典型的访存受限（memory-bound）负载。",
                    "基线的 `float xi = x[i]; float yi = y[i]; y[i] = yi + a*xi;` 引入了额外的寄存器拷贝，也让数据流对编译器不够直观。",
                    "标量实现下，循环控制指令（比较、自增、跳转）占比可观。",
                ]},
                {"h": "优化思路", "p": [
                    "写成最直白的 `y[i] = a * x[i] + y[i];`，去掉中间临时变量。",
                    "8 路展开：一次迭代处理 8 个元素，给编译器明确的\"可成组处理\"信号，同时摊薄循环开销。",
                    "确保没有别名歧义：x 与 y 是不同数组，若有重叠顾虑可用 `restrict` 明确告知编译器。",
                ]},
                {"h": "关键改动", "p": [
                    "主循环 `for (; i <= n - 8; i += 8)`，体内 8 行独立的 `y[i+k] = a*x[i+k] + y[i+k];`。",
                    "尾部循环处理余数。",
                    "全程无分支、无函数调用、无指针别名歧义。",
                ]},
                {"h": "为什么更快", "p": [
                    "编译器把 8 行语句打包成若干条向量乘加指令，退休指令数大幅下降。",
                    "8 次迭代共享一次比较与跳转，前端压力显著降低。",
                    "由于是访存受限，IPC 的提升主要来自\"同样周期内完成更多元素\"，而非计算单元被填满。",
                ]},
                {"h": "常见误区", "p": [
                    "以为展开就等于向量化：展开只是为向量化创造条件，是否真生成 SIMD 要看编译器报告（`-fopt-info-vec`）。",
                    "忽略别名：若编译器无法证明 x、y 不重叠，它会生成运行时检查或干脆放弃向量化；此时 `restrict` 是关键。",
                    "访存受限时向量化收益有限：本题瓶颈在带宽，把 SIMD 宽度再翻倍也不会线性提速。",
                ]},
            ],
            "keypoints": ["去掉临时变量让数据流清晰", "8 路展开 + 无分支", "必要时用 restrict 消除别名顾虑"],
            "pitfalls": ["访存受限时收益有天花板", "展开不等于向量化"],
        },
    },
    "gather-reorder-cache-clustering": {
        "zh": "聚集重排：按页聚簇访问",
        "one_liner": "用计数排序把随机索引按页分组，让访存在物理页上聚集。",
        "bottleneck": "页级（TLB）与缓存局部性",
        "desc": [
            "用一组索引置换从一个大数组中求和。基线按到达顺序遍历置换，于是每次迭代都可能跳到数据数组的完全不同的区域。",
            "数据数组 512KB、索引随机散布，多数访问落在冷缓存行上，CPU 几乎每次迭代都在等内存。",
            "请在内部对访问重排，让指向同一区域的索引彼此靠近，然后再执行聚集。",
        ],
        "prompt": "实现 `long solve(const int *data, const int *indices, int n)`，精确返回所有 `data[indices[i]]` 之和，同时通过在聚集前重新分组访问来改善缓存局部性。",
        "constraints": ["n = 131072（评测中固定）", "indices[] 是 0..n-1 的一个置换", "每个被引用元素必须恰好访问一次", "不允许多线程", "结果必须精确"],
        "examples": [{
            "input": "data = 512KB int 数组；indices = 随机置换",
            "output": "全部被引用元素之和",
            "explanation": "求和与顺序无关，因此可以自由重排访问顺序 —— 这正是本题留给你的优化空间。",
        }],
        "report": {
            "summary": "用**计数排序**（三趟：统计、前缀和、散布）把索引按所属 4KB 页重新分组，然后按新顺序顺序聚集。访问于是从\"全数组随机跳\"变成\"逐页顺序扫\"。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "512KB 数据在 4KB 页下跨越 128 个页面；随机访问意味着每次迭代都可能换页。",
                    "结果是双重的：缓存行冷（L1 未命中）且 TLB 条目被反复冲刷（页表遍历开销叠加在缓存访问之上）。",
                    "单纯预取对此帮助有限 —— 因为访问顺序本身就是发散的。",
                ]},
                {"h": "优化思路", "p": [
                    "求和满足交换律，访问顺序可以自由改变。",
                    "按页分组：`page = index / 1024`（4096 字节 / 4 字节）。用计数排序把同页的索引聚到一起。",
                    "重排后，聚集阶段在一段时间内只访问同一个 4KB 页内的地址 —— 该页的 TLB 条目与缓存行都能被充分利用。",
                ]},
                {"h": "关键改动", "p": [
                    "第一趟：统计每个页包含多少个索引 `counts[indices[i] / 1024]++`。",
                    "第二趟：前缀和得到每页在输出数组中的起始偏移 `offsets[]`。",
                    "第三趟：散布 `ordered[offsets[page]++] = indices[i]`。",
                    "注意实现细节：散布后 `offsets` 已被推进，需要再后移一格还原，才能正确表达每页起始位置（参考实现中用了倒序 shift 的技巧）。",
                    "最后按 `ordered` 顺序做一次完全顺序的聚集。",
                ]},
                {"h": "为什么更快", "p": [
                    "页级局部性：同一页的约 1024 个索引被连续处理，TLB 只需一次装填即可服务上千次访问。",
                    "缓存局部性：页内 4KB 恰好是 L1 可容纳的量级，页内访问能反复命中。",
                    "代价是三趟 O(n) 预处理与 O(n) 额外内存 —— 相比 13 万次随机访存的节省，完全值得。",
                ]},
                {"h": "常见误区", "p": [
                    "以为要按缓存行（64B）分组：过细的分组会让分组本身开销过大；按页分组在 TLB 收益与开销之间取得平衡。",
                    "忘记还原 `offsets`：散布循环中 `offsets[page]++` 修改了数组，若直接复用会导致后续逻辑错位。",
                    "忽略额外内存：需要 counts、offsets、ordered 三个数组，内存紧张时需评估。",
                ]},
            ],
            "keypoints": ["计数排序按页聚簇", "同时改善 TLB 与缓存局部性", "三趟 O(n) 预处理的代价可控"],
            "pitfalls": ["offsets 被推进后需还原", "分组粒度需权衡"],
        },
    },
    "masked-saxpy-branchless": {
        "zh": "掩码 SAXPY：去分支换取吞吐",
        "one_liner": "mask 为 0/1 —— 直接把它当乘数，分支就消失了。",
        "bottleneck": "不可预测分支 + 阻碍向量化",
        "desc": [
            "执行带掩码的 SAXPY 更新：若 `mask[i]` 为 1 则 `y[i] = a*x[i] + y[i]`，否则 `y[i]` 保持不变。",
            "基线对每个元素使用一次分支。掩码大致 50/50 时，该分支难以预测，也让热循环对向量化不友好。",
            "请重写循环，在保持结果精确的前提下做到无分支且面向吞吐。",
        ],
        "prompt": "实现 `void solve(float a, const float *x, float *y, const unsigned char *mask, int n)`，精确执行掩码 SAXPY 更新。",
        "constraints": ["n = 262144（评测中固定）", "mask[i] 取值只能为 0 或 1", "不允许多线程", "允许 SIMD", "浮点容差：1e-4"],
        "examples": [{
            "input": "x、y 为 262,144 个 float；mask 为 0/1 字节数组",
            "output": "按掩码更新后的 y",
            "explanation": "当 mask 为 0 时 `y[i] += (a*x[i]) * 0.0f` 等价于加零 —— 除 -0.0 与 NaN 等极端情形外与不更新等价。",
        }],
        "report": {
            "summary": "把 `if (mask[i])` 改写成 `y[i] += (a * x[i]) * (float)mask[i];`，用一次乘法实现\"条件执行\"，再叠加 8 路展开。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "掩码 50/50 时分支预测器退化为抛硬币，Branch MPKI 可达数十。",
                    "分支还把循环切成\"每次迭代可能跳过工作\"的形式，编译器无法安全向量化。",
                    "结果是双重损失：既付了误预测的代价，又拿不到 SIMD 的收益。",
                ]},
                {"h": "优化思路", "p": [
                    "mask 只有 0/1 两个取值 —— 直接把它转成 float 作为乘数：`mask ? 1.0f : 0.0f`。",
                    "于是 `y[i] += (a * x[i]) * (float)mask[i]` 在 mask=0 时等价于加 0，在 mask=1 时等价于正常更新。",
                    "无分支的直线代码可以被安全地向量化。",
                ]},
                {"h": "关键改动", "p": [
                    "删除 `if (mask[i])`，改为 8 行 `y[i+k] += (a * x[i+k]) * (float)mask[i+k];`。",
                    "8 路展开，尾部循环处理余数。",
                    "把 `y[i] = a*x[i] + y[i]` 改成 `y[i] += ...` 形式，配合乘 0 实现\"保持不变\"。",
                ]},
                {"h": "为什么更快", "p": [
                    "Branch MPKI 降到接近 0，前端不再被误预测打断。",
                    "向量化后每周期处理多个元素，IPC 提升到 3 以上。",
                    "代价：所有元素都要做乘法与加法，包括 mask=0 的那些 —— 这是用\"多做算术\"换\"不赌分支\"。",
                ]},
                {"h": "常见误区", "p": [
                    "认为无分支永远更快：若掩码 99% 为 0，带分支的版本会跳过大部分工作，反而更快。判断是否划算要看 `分支误预测率 × 15 周期` 与 `多做的算术周期数` 谁更大。",
                    "-0.0 与 NaN 的边角：加 0.0 会把 -0.0 变成 +0.0，也会传播 NaN；本题容差 1e-4 下可接受，但对逐位一致的场景要谨慎。",
                    "忘记 `(float)` 转换：若 mask 是 unsigned char，需确保转换到 float 而非整数乘。",
                ]},
            ],
            "keypoints": ["掩码当乘数实现条件执行", "为向量化扫清障碍", "8 路展开"],
            "pitfalls": ["掩码高度偏斜时分支版本更快", "-0.0 / NaN 语义差异"],
        },
    },
    "dot-product-eight-accumulators": {
        "zh": "点积：八个累加器",
        "one_liner": "一条长为 N 的浮点依赖链 → 8 条长为 N/8 的链。",
        "bottleneck": "浮点累加依赖链",
        "desc": [
            "计算两个大规模 float 数组的点积。基线使用单个累加器，形成一条很长的浮点依赖链。",
            "即便数据已经很好地从内存流式供给，这条链依然限制着指令级并行。",
            "请用多个彼此独立的局部和打破它，最后再合并。",
        ],
        "prompt": "实现 `float solve(const float *a, const float *b, int n)`，返回两个 float 数组的点积，且比单累加器循环暴露出更多 ILP。",
        "constraints": ["n = 262144（评测中固定）", "不允许多线程", "允许 SIMD", "浮点容差：1e-3"],
        "examples": [{
            "input": "a、b 为 262,144 个 float",
            "output": "点积 a·b",
            "explanation": "浮点加法延迟约 4 周期，单累加器把 IPC 锁死在 0.25；8 个累加器理论上把它抬到 2 以上。",
        }],
        "report": {
            "summary": "8 路展开 + 8 个独立累加器 + 树形合并：把关键路径缩短为 1/8，收尾阶段也用树形结构减少累加延迟。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "`acc += a[i] * b[i]` 中每次加法依赖上一次加法 —— 一条长度 N 的 RAW 链。",
                    "浮点加法延迟约 4 周期，因此单累加器每 4 周期才能完成一次乘加，IPC 上限约 0.25。",
                    "乘法本身可以并行（`a[i]*b[i]` 之间无相关），被拖累的只有累加。",
                ]},
                {"h": "优化思路", "p": [
                    "引入 s0..s7 八个累加器，按下标模 8 分配相位。",
                    "八条链互不相关，乱序核可以在等待 s0 时发射 s1..s7 的加法。",
                    "收尾时用 `(s0+s1)+(s2+s3)+(s4+s5)+(s6+s7)` 的树形结构，把 8 次串行加法压成 3 层。",
                ]},
                {"h": "关键改动", "p": [
                    "主循环 `for (; i <= n - 8; i += 8)`，体内八行独立 `sK += a[i+k] * b[i+k];`。",
                    "尾部循环把余数累加到 s0。",
                    "返回值用树形括号，明确表达合并顺序。",
                ]},
                {"h": "为什么更快", "p": [
                    "关键路径缩短为 1/8，IPC 从 0.25 提升到 3.4 以上。",
                    "树形收尾把 7 次串行加法（28 周期）压成 3 层（12 周期）。",
                    "需注意：8 个 float 累加器需占用 8 个向量/浮点寄存器，现代 x86 有 16 个 YMM 寄存器，尚在安全范围；再往上就要小心溢出。",
                ]},
                {"h": "常见误区", "p": [
                    "浮点重排改变结果：本题容差 1e-3 允许多累加器；若要求与参考逐位一致则不能这样做（除非加 `-ffast-math`，但那会影响全局）。",
                    "累加器越多越好：超过物理寄存器会 spill 到栈，性能断崖式下降。8 是本题在 8 发射核上的合理取值。",
                    "忽略尾部：n 不一定是 8 的倍数，必须有收尾循环。",
                ]},
            ],
            "keypoints": ["8 累加器拆依赖链", "树形合并减少收尾延迟", "寄存器用量需在安全范围"],
            "pitfalls": ["浮点精度受容差约束", "累加器过多会 spill"],
        },
    },
    "lookup-table-grade-bands": {
        "zh": "等级分档：用查表替代嵌套分支",
        "one_liner": "分值只有 0–100 —— 那就把 101 种结果预先算好。",
        "bottleneck": "嵌套分支的控制流开销",
        "desc": [
            "在大量重复轮次中把分数归类到等级区间。基线对每一轮的每一个分数都走一遍嵌套 if/else 树。",
            "当分数在输入范围内广泛分布时，控制流的可预测性远不如它应有的水平。",
            "请预计算一张很小的查找表，然后把热循环简化为一次查表与累加。",
        ],
        "prompt": "实现 `long solve(const unsigned char *scores, int n)`，返回 200 轮的加权总和，其中分数按等级区间映射为权重。",
        "constraints": ["n = 100000（评测中固定）", "scores[i] 取值范围 0..100", "允许预计算查找表", "不允许多线程", "结果必须精确"],
        "examples": [{
            "input": "scores = 100,000 个 0–100 的分数",
            "output": "200 轮的加权总和（<60→1，<70→2，<80→4，<90→8，否则 16）",
            "explanation": "分数只有 101 种可能取值，却要被判断 200×100,000 = 2000 万次 —— 典型的\"重复计算同样结果\"。",
        }],
        "report": {
            "summary": "预计算 `lut[256]`，把 5 层 if/else 压缩成热循环中的**一次字节查表** `total += lut[scores[i]]`。建表成本 O(101)，被 2000 万次查表完全摊薄。",
            "sections": [
                {"h": "瓶颈定位", "p": [
                    "基线每元素要顺序测试最多 5 个条件，产生最多 5 次分支；实际平均约 3 次。",
                    "2000 万次元素处理 × 约 3 次分支 = 约 6000 万次分支判断，即便每次只有少量误预测，累积代价也很可观。",
                    "更关键的是：这些分支的结果**只取决于 101 种可能的分数**，完全是在反复计算同样的答案。",
                ]},
                {"h": "优化思路", "p": [
                    "识别\"小值域 + 重复计算\"这一模式：输入只有 101 种取值，输出也只有 5 种权重。",
                    "预计算 `lut[s]` 把「分数 → 权重」的映射固化成一次内存查表。",
                    "热循环于是退化为 `total += lut[scores[i]]` —— 无分支、无复杂控制流。",
                ]},
                {"h": "关键改动", "p": [
                    "建表循环保留原来的 if/else 逻辑（只执行 101 次，写法无需优化）。",
                    "`unsigned char lut[256]`：256 字节稳稳驻留 L1 的一角，查表几乎永远命中。",
                    "热循环内删除全部条件判断。",
                ]},
                {"h": "为什么更快", "p": [
                    "分支数量从平均每元素约 3 次降到 0，Branch MPKI 大幅下降。",
                    "每元素的指令数从约 6–10 条降到约 3 条（取字节、零扩展、查表、加），前端压力骤减。",
                    "查表只有 256 字节，完全驻留 L1，访存延迟可忽略；建表开销 101 次相对 2000 万次完全可忽略。",
                ]},
                {"h": "常见误区", "p": [
                    "表开得过大：若值域是 32 位整数就不能直接建表，需要改成分段查表或哈希 —— 本题能建表的前提是\"分数 ∈ [0,100]\"。",
                    "忽略初始化：`lut` 必须初始化（参考实现用 `= {0}`），且只填了 0..100；若输入可能超出该范围则需填满 256 项。",
                    "在只遍历一轮时照搬：建表的收益来自被大量重复调用摊薄；只跑一轮时收益为负。",
                ]},
            ],
            "keypoints": ["识别小值域 + 重复计算模式", "256 字节表常驻 L1", "建表成本被 200 轮摊薄"],
            "pitfalls": ["值域过大时查表不适用", "表必须完整初始化"],
        },
    },
}
